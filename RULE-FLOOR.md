FLOOR: 141
RED-PROOFS: 141

| ID | one-sentence rule | enforced-by | check | red-proof | hash |
|---|---|---|---|---|---|
| SETUP-CODE-1 | A fresh appliance prints a one-time setup code and refuses after setup completes | go-test | cmd/identuum-idp/setup_show_code_test.go @ unit | mutation red-proved 2026-08-18: runShowSetupCode setup-already-complete refusal neutralized (false &&), watched FAIL (stale setup token printed after completion, rc 0 not 2), restored byte-identical | 7b9993252c7c |
| RECOVER-1 | recover-site-admin resets only the sentinel-guarded site_admin row | go-test | cmd/identuum-idp/recover_test.go @ unit | mutation red-proved 2026-08-18: recoverSiteAdminCore sentinel-ID mismatch guard neutralized (false &&), watched FAIL (recovery updated a drifted non-sentinel row instead of refusing), restored byte-identical | aaca81397daf |
| LOCKOUT-1 | Lockout answers exactly like a wrong password: invalid_credentials | go-test | internal/handlers/login_risk_unavailable_test.go @ unit | mutation red-proved 2026-08-18: LocalLoginService.Login genuine-lockout branch returns ErrLoginRiskBackendUnavailable instead of ErrLoginInvalidCredentials, watched FAIL (lockout surfaced as 503 revealing lockout state instead of 401 invalid_credentials), restored byte-identical | 6607b26ad69a |
| LOGIN-PIN-1 | Login identity is site_admin@system.local; contact_email never logs in | go-test | internal/setup/rg15_login_identity_test.go @ unit | mutation red-proved 2026-08-18: siteAdminIdentity returned the operator address as the login, watched FAIL (login not pinned to site_admin@system.local), restored byte-identical | e656142e21f9 |
| R1 | The pinned model values are the shared OSS/CE contract. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SystemOrgSlug diverged from the shared CE literal (system-local-x), watched FAIL (rule-one divergence), restored byte-identical | fd3b666ad93f |
| R4 | The System organization has the lowest possible UUIDv7 id. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SystemOrgID sentinel moved 0000->0002, watched FAIL (SystemOrgID want v7-zero), restored byte-identical | b25844fef644 |
| R5 | The site_admin has the lowest possible UUIDv7 id plus one. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SiteAdminID sentinel moved 0001->0003, watched FAIL (SiteAdminID want v7-zero+1), restored byte-identical | ae0102bb1983 |
| R6 | The site_admin login identifier constant is site_admin@system.local. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SiteAdminEmail constant changed to admin@system.local, watched FAIL (login identifier drifted), restored byte-identical | e438a0bd3876 |
| R8 | The System Organization name and slug are pinned to System Organization and system-local. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SystemOrgName changed to System Org, watched FAIL (name drifted), restored byte-identical | 99a5118b530e |
| R16 | The org_admin scope set covers exactly the granted admin areas. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: ScopeUsersRead dropped from OrgAdminSessionScopes, watched FAIL (granted area users omitted), restored byte-identical | 9f0806e3c23c |
| R18 | site_admin is not an assignable organization role. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: ScopeAuditRead value made site-shaped (site:audit:read) while granted to org_admin, watched FAIL (site-shaped scope held), restored byte-identical | 047adcbd036a |
| R21 | org_user carries no administrative scopes. | go-test | internal/domain/r_suite_test.go @ unit | mutation red-proved 2026-08-18: SessionScopesForRole extended to RoleOrgUser, watched FAIL (org_user session carries admin scopes), restored byte-identical | 4c672323260a |
| MODEL-SYSJOIN-1 | Only the site_admin may be a member of the System organization. | go-test | internal/service/model_sentinel_guards_test.go @ unit | mutation red-proved 2026-08-18: System-org membership guard neutralized (false &&), watched FAIL (org_user and org_admin created in System org), restored byte-identical | 9dcd37ab8724 |
| MODEL-DELETABLE-1 | Ordinary users stay deletable; sentinel protection covers only the sentinels. | go-test | internal/service/model_sentinel_guards_test.go @ unit | mutation red-proved 2026-08-18: UserService.Delete sentinel guard over-reached to every user (guard condition forced true), watched FAIL (ordinary org_admin no longer deletable), restored byte-identical | 43ad8f009e33 |
| MODEL-SA-NODELETE-1 | The site_admin cannot be deleted by any actor. | go-test | internal/service/model_sentinel_guards_test.go @ unit | mutation red-proved 2026-08-18: DeleteUserForActor sentinel guard neutralized (false && wrap), watched FAIL (site_admin deleted via actor path), restored byte-identical | ad1ed1db3c6f |
| MODEL-SYSORG-NODELETE-1 | The System organization cannot be deleted. | go-test | internal/service/model_sentinel_guards_test.go @ unit | mutation red-proved 2026-08-18: System-org delete guard neutralized (false &&), watched FAIL (delete returned nil error, want ErrForbidden), restored byte-identical | fcb5dfba5a60 |
| MODEL-SYSORG-NORENAME-1 | The System organization cannot be renamed. | go-test | internal/service/model_sentinel_guards_test.go @ unit | mutation red-proved 2026-08-18: System-org rename guard neutralized (false &&), watched FAIL (rename returned nil error, want ErrForbidden), restored byte-identical | f31693aa103b |
| MODEL-404-SA-1 | An org_admin sees the site_admin account as 404, never 403. | go-test | internal/handlers/model_404_test.go @ unit | mutation red-proved 2026-08-18: GetUserForActor boundary miss flipped errUserNotFound to ErrForbidden, watched FAIL (site_admin read answered 403, want 404), restored byte-identical | fcfb763ce73f |
| MODEL-404-CROSSORG-1 | An org_admin sees another organization's user as 404, never 403. | go-test | internal/handlers/model_404_test.go @ unit | mutation red-proved 2026-08-18: GetUserForActor same-org predicate dropped, watched FAIL (cross-org read answered 200, want 404), restored byte-identical | fc751e8cbfbc |
| TRIO-OWN-1 | An org_admin can create users in their own organization. | go-test | internal/handlers/session_scope_trio_test.go @ unit | mutation red-proved 2026-08-18: CreateUserForActor org_admin branch disabled (case false &&), watched FAIL (own-org create answered 403, want 201), restored byte-identical | beeb7efbca7e |
| TRIO-OTHER-1 | An org_admin acting on another organization gets 403. | go-test | internal/handlers/session_scope_trio_test.go @ unit | mutation red-proved 2026-08-18: org_admin cross-org refusal neutralized (false &&), watched FAIL (cross-org create answered 201, want 403), restored byte-identical | c00ec4f58f37 |
| TRIO-USER-1 | An org_user attempting admin actions gets 403. | go-test | internal/handlers/session_scope_trio_test.go @ unit | mutation red-proved 2026-08-18: all three enforcing layers neutralized at once (mw role check, mw scope check, service default branch), watched FAIL (org_user create answered 400 not 403 - the guard 403 vanished), restored byte-identical | 8068a8b474b7 |
| SA-TENANT-FIRST-1 | site_admin may seed a tenant organization's first org_admin. | go-test | internal/service/site_admin_tenant_write_test.go @ unit | mutation red-proved 2026-08-18: zero-admins predicate tightened to refuse everything (existing >= 0), watched FAIL (first org_admin delegation refused), restored byte-identical | 39bd8e883964 |
| SA-TENANT-SECOND-1 | site_admin cannot add a second org_admin once one is active. | go-test | internal/service/site_admin_tenant_write_test.go @ unit | mutation red-proved 2026-08-18: zero-admins predicate widened (existing > 1), watched FAIL (second org_admin created), restored byte-identical | 5e33a3079876 |
| SA-TENANT-USER-1 | site_admin cannot create a regular org_user inside a tenant. | go-test | internal/service/site_admin_tenant_write_test.go @ unit | mutation red-proved 2026-08-18: guardSiteAdminTenantWrite role check neutralized (false &&), watched FAIL (site_admin created an org_user in a tenant), restored byte-identical | f5f18254dc0d |
| SA-TENANT-SYSORG-1 | The System organization is not a tenant for delegation purposes. | go-test | internal/service/site_admin_tenant_write_test.go @ unit | mutation red-proved 2026-08-18: system-org exemption dropped from the tenant guard (false &&), watched FAIL (tenant rule leaked into the System organization), restored byte-identical | 8134de2b8c57 |
| RG10 | Cross-org write probes are indistinguishable from a miss. | go-test | internal/handlers/rg10_no_existence_oracle_test.go @ unit | mutation red-proved 2026-08-18: ResetMFAForActor cross-org miss flipped errUserNotFound to ErrForbidden, watched FAIL (foreign 403 vs absent 404 - the oracle reopened), restored byte-identical | 4992c27d5a20 |
| CONTACT-EMAIL-1 | The installer's typed address is stored as contact email, trimmed and optional. | go-test | internal/setup/rg15_login_identity_test.go @ unit | mutation red-proved 2026-08-18: siteAdminIdentity stopped trimming the contact address, watched FAIL (contact kept its whitespace), restored byte-identical | 72a17995a3af |
| P0-REFRESH-1 | A reused refresh token is detected and revokes exactly its token family. | go-test | internal/service/refresh_token_family_test.go @ unit | mutation red-proved 2026-08-18: rotated-token reuse branch returns ErrRefreshTokenInvalidGrant instead of ErrRefreshTokenReuse, watched FAIL (replay of ta1 not detected as reuse), restored byte-identical | 7a4254a50927 |
| P0-JTI-1 | Bearer JTI revocation fails closed: a store error rejects the token. | go-test | internal/mw/bearer_revocation_test.go @ unit | mutation red-proved 2026-08-18: BearerPrincipal resolver fail-closed branch neutralized (false &&), watched FAIL (session-store error admitted the token, status 200 not 401), restored byte-identical | ce337e4e2118 |
| P0-CLIAUTH-1 | A client authenticates only with its exact registered token-endpoint method. | go-test | internal/service/oauth_client_auth_service_test.go @ unit | mutation red-proved 2026-08-18: Authenticate exact-registered-method check neutralized (false &&), watched FAIL (basic-registered client authenticated via post), restored byte-identical | 7f2a12978f45 |
| P0-TENANT-1 | Organization liveness decisions use the single IsOperational predicate. | go-test | internal/domain/domain_methods_test.go @ unit | mutation red-proved 2026-08-18: Organization.IsOperational dropped the DeletedAt==nil conjunct, watched FAIL (soft-deleted-but-active case returned true, want false), restored byte-identical | 2a32ce42024f |
| P0-BEARER-1 | Resource APIs authenticate only via Authorization Bearer; no header is 401. | go-test | internal/mw/bearer_test.go @ unit | mutation red-proved 2026-08-18: BearerPrincipal empty-header branch now calls VerifyBearerToken before falling through, watched FAIL (verifier called with no header, calls=1 want 0), restored byte-identical | 7f54833e14d7 |
| AUTH-WRONGPW-1 | A wrong password is refused with the generic 401 invalid_credentials. | go-test | internal/handlers/auth_sessions_test.go @ unit | mutation red-proved 2026-08-18: emitLoginError invalid_credentials error string changed to invalid_login, watched FAIL (wrong-password 401 body no longer says invalid_credentials), restored byte-identical | e9156be44ea7 |
| USER-BAN-LOGIN-1 | A banned user's login collapses to invalid_credentials. | go-test | internal/service/local_login_service_test.go @ unit | mutation red-proved 2026-08-18: pickSingleActiveUser Banned-skip neutralized (false &&), watched FAIL (banned user with correct password no longer collapsed to invalid_credentials), restored byte-identical | 31678701a8dd |
| INTROSPECT-REVOKED-1 | A revoked token introspects as active false. | go-test | internal/service/introspection_service_test.go @ unit | mutation red-proved 2026-08-18: Introspect revoked-jti flip-to-inactive neutralized (false &&), watched FAIL (revoked jti returned active:true), restored byte-identical | 2d153e4dc3ea |
| INTROSPECT-AUTH-1 | Introspection refuses an unauthenticated client with 401. | go-test | internal/handlers/introspection_test.go @ unit | mutation red-proved 2026-08-18: respondInvalidClient status changed 401 to 400 on the client-auth reject path, watched FAIL (introspection client-auth rejection answered 400 not 401), restored byte-identical | e612bc45fd7b |
| MFA-RECOVERY-1 | Recovery codes are stored hashed and each is accepted exactly once. | go-test | internal/service/mfa_at_rest_test.go @ unit | mutation red-proved 2026-08-18: hashRecoveryCodes stored the raw plaintext instead of crypto.HashSecret, watched FAIL (recovery code stored RAW at rest), restored byte-identical | b1d4f226db31 |
| RATE-TOKEN-1 | Past the per-IP limit the rate-limited endpoint answers 429. | go-test | internal/mw/rate_limit_test.go @ unit | mutation red-proved 2026-08-18: rate-limit gate forced to always pass (allow short-circuited true), watched FAIL (second over-limit request answered 200 not 429), restored byte-identical | 4b94877cb698 |
| AUTHCODE-REPLAY-1 | An authorization code is single-use; replay revokes what it minted. | go-test | internal/service/authcode_reuse_revocation_test.go @ unit | mutation red-proved 2026-08-18: authcode read-side replay revokeOnReuse call neutralized (false &&), watched FAIL (replayed code did not revoke what the first exchange minted, revoker calls=0 want 1), restored byte-identical | 6da8c2f64474 |
| MFA-ADMIN-ENROLL-1 | An admin without MFA gets enrollment_required and no cookies. | go-test | internal/handlers/auth_mfa_policy_test.go @ unit | mutation red-proved 2026-08-18: emitLoginError mfa_enrollment_required error string changed to enroll_mfa, watched FAIL (org_admin-without-MFA 401 body no longer says mfa_enrollment_required), restored byte-identical | f1abf989ab94 |
| MFA-EVERY-LOGIN-1 | An MFA-enabled admin logging in without a code gets mfa_required and no cookies. | go-test | internal/handlers/auth_mfa_policy_test.go @ unit | mutation red-proved 2026-08-18: emitLoginError mfa_required body rewritten (error and boolean key renamed to totp), watched FAIL (MFA-enabled admin missing-code 401 body no longer says mfa_required), restored byte-identical | 3dfd49389c4a |
| RG1 | The System organization cannot be suspended or soft-deleted at the database layer. | go-test | internal/postgres/model_update_teeth_test.go @ integration | 2026-08-17 mutated migration 0028 model_protect_system_org (IF FALSE AND on active+deleted_at guards), applied live via psql; TestRg1 FAIL 'suspending the System organization SUCCEEDED'; source restored byte-identical, original trigger re-applied, green | 48ccc6609a82 |
| RG2 | The site_admin cannot be banned, demoted, or soft-deleted at the database layer. | go-test | internal/postgres/model_update_teeth_test.go @ integration | 2026-08-18 mutated migration 0028 model_protect_site_admin_update (IF FALSE AND on role+deleted_at+banned+organization_id guards), applied live via psql; TestRg2 FAIL 'banning the site_admin SUCCEEDED' and 'soft-deleting SUCCEEDED'; source restored byte-identical, guard+sentinel healed, green | f436fd3a3786 |
| USER-ORG-1 | Every user belongs to exactly one organization. | go-test | internal/service/burndown_rules_test.go @ unit | mutation red-proved 2026-08-18: defense-in-depth 2 layers - CreateUserForActor org-id guard AND User.Validate org-id check both neutralized (false &&), watched FAIL (orgless create not refused), restored byte-identical | 4a42217b440a |
| SA-SINGLETON-1 | There is exactly one site_admin per installation. | go-test | cmd/identuum-idp/bootstrap_test.go @ unit | mutation red-proved 2026-08-18: bootstrapCore existing-site_admin short-circuit neutralized (false &&), watched FAIL (3 invocations no longer idempotent - more than one site_admin), restored byte-identical | 49670a09a4f2 |
| SA-IN-SYSORG-1 | The site_admin belongs to the System organization. | go-test | cmd/identuum-idp/bootstrap_test.go @ unit | mutation red-proved 2026-08-18: bootstrapCore created the site_admin with OrganizationID=siteAdminID instead of systemOrgID, watched FAIL (site_admin not a member of the System organization), restored byte-identical | 094c9ac3636b |
| ORG-MOVE-1 | No actor may move a user across organizations. | go-test | internal/service/burndown_rules_test.go @ unit | mutation red-proved 2026-08-18: an OrganizationID pointer field added to service.UpdateUserOptions, watched FAIL (field count 9 want pinned 8 - update surface grew a cross-org mover), restored byte-identical | dc07a268184d |
| ORG-LIFECYCLE-1 | Organization lifecycle is infrastructure authority only. | go-test | internal/handlers/identity_admin_batch_test.go @ unit | mutation red-proved 2026-08-18: defense-in-depth 3 layers - mw org-id-nil check, mw scope check, and HandleListOrganizations org-id-nil check all neutralized (false &&), watched FAIL (org_admin GET /organizations answered 200 not 403), restored byte-identical | d35704f0cd6c |
| ORG-CREATE-403-1 | An org_user is refused organization creation with 403. | go-test | internal/handlers/identity_admin_batch_test.go @ unit | mutation red-proved 2026-08-18: RequireSiteAdmin site-admin check neutralized (false &&), watched FAIL (org_user POST /organizations answered 201 not 403), restored byte-identical | 6769c9a718ef |
| ORG-RESTORE-1 | A soft-deleted organization 404s on read yet remains restorable. | go-test | internal/handlers/identity_admin_batch_test.go @ unit | mutation red-proved 2026-08-18: OrganizationService.Restore made a no-op (Undelete not called), watched FAIL (get after restore answered 404 not 200 - the org stayed soft-deleted), restored byte-identical | 86d8661015b4 |
| USER-PW-REQUIRED-1 | User creation without a password fails with a clear 4xx. | go-test | internal/service/burndown_rules_test.go @ unit | mutation red-proved 2026-08-18: defense-in-depth 2 layers - Create empty-password guard AND ValidatePasswordPolicy both neutralized (false &&), watched FAIL (passwordless create not refused), restored byte-identical | 2515a32f857f |
| USER-RESET-TOKEN-1 | A password reset with a bogus token is refused. | go-test | internal/service/password_reset_service_test.go @ unit | mutation red-proved 2026-08-18: ResetPassword bogus-token branch returns nil instead of ErrPasswordResetInvalidToken, watched FAIL (bad token accepted), restored byte-identical | 9e846d66c467 |
| USER-VERIFY-TOKEN-1 | Email verification with a bogus token is refused. | go-test | internal/service/email_verification_service_test.go @ unit | mutation red-proved 2026-08-18: VerifyEmail bogus-token branch returns nil instead of ErrEmailVerificationInvalidToken, watched FAIL (bad token accepted), restored byte-identical | 001799e39a8b |
| SECRET-ONCE-1 | A client secret is returned exactly once and never re-shown. | go-test | internal/service/oauth_admin_services_test.go @ unit | mutation red-proved 2026-08-18: prepareClient stored the raw plaintext as the secret hash (HashSecret dropped), watched FAIL (stored hash equals plaintext), restored byte-identical | fa646c4eb832 |
| PKCE-S256-1 | Discovery advertises S256 as the only PKCE method. | go-test | internal/api/oauth_discovery_test.go @ unit | mutation red-proved 2026-08-18: discovery code_challenge_methods_supported gained plain alongside S256, watched FAIL (advertised methods no longer S256-only), restored byte-identical | c937e5e5d461 |
| TOKEN-SPLIT-1 | The token endpoint refuses a user-session refresh token with invalid_grant. | go-test | internal/handlers/token_test.go @ unit | mutation red-proved 2026-08-18: token endpoint refresh-grant invalid_grant mapping changed to invalid_request, watched FAIL (unknown user-session refresh token no longer answered invalid_grant), restored byte-identical | 7d116bcf83f6 |
| MFA-BADCODE-1 | A bad TOTP code is refused. | go-test | internal/service/mfa_enrollment_service_test.go @ unit | mutation red-proved 2026-08-18: VerifyAndConsume TOTP check forced to accept (totpOK short-circuited true), watched FAIL (wrong TOTP code no longer rejected with ErrMFAEnrollmentInvalid), restored byte-identical | 3b3e9c634b7d |
| CLIENT-SCOPE-1 | An org_admin's client list is bound to their own organization only. | go-test | internal/handlers/clients_org_bound_test.go @ unit | mutation red-proved 2026-08-18: orgAdminClientScope org_admin branch returns nil instead of the actor org filter, watched FAIL (org-bound actor got NO filter and would see every tenants clients), restored byte-identical | dc8743424e1d |
| SECRET-ROTATE-1 | A secret rotation returns the new plaintext exactly once and rotates the stored hash. | go-test | internal/service/oauth_admin_services_test.go @ unit | mutation red-proved 2026-08-18: RegenerateClientSecret dropped the ClientSecretHash reassignment (hash left stale), watched FAIL (hash did not rotate), restored byte-identical | 389e549f5cc3 |
| NO-FATAL-1 | A fatal wiring fault is recorded, never a panic: the process degrades to not-serving. | go-test | internal/service/classc_failsafe_test.go @ unit | mutation red-proved 2026-08-18: NewUserSessionService nil-repo path replaced report.Fatal with panic(), watched FAIL (constructor panicked instead of recording a fatal fault), restored byte-identical | 4637321cbe12 |
| ROLE-ASSIGN-SCOPE-1 | Role assignment obeys the same tenant authority line as user creation. | go-test | internal/service/org_role_user_tenant_guard_test.go @ unit | mutation red-proved 2026-08-18: AssignRoleToUserForActor cross-org target check neutralized (false &&), watched FAIL (cross-org role assign returned nil not ErrForbidden), restored byte-identical | 60f9dc142fdf |
| OIDC-STATE-1 | An upstream OIDC login state is single-use; replay is refused. | go-test | internal/service/oidc_callback_service_test.go @ unit | mutation red-proved 2026-08-18: HandleCallback swapped the consuming ConsumeByState for the non-consuming Get, watched FAIL (state not consumed after first callback and replay accepted), restored byte-identical | 0c1a4ed926bb |
| OIDC-JIT-GATE-1 | JIT provisioning refuses a non-allowlisted email domain before any account exists. | go-test | internal/service/oidc_callback_jit_test.go @ unit | mutation red-proved 2026-08-18: resolveLocalUser email-domain allow-list gate neutralized (false &&), watched FAIL (off-allow-list email provisioned a user), restored byte-identical | a764490b9f5e |
| OIDC-TAKEOVER-1 | Upstream identity matching goes by external id before email; an email change cannot take over another account. | go-test | internal/service/oidc_callback_jit_test.go @ unit | mutation red-proved 2026-08-18: resolveLocalUser ExternalID-first match branch neutralized (false &&), watched FAIL (returning user resolved to victim by changed email instead of own account by ExternalID), restored byte-identical | 182ed2bb8666 |
| RG3 | The DB refuses System-org rename, slug and id changes. | go-test | internal/postgres/model_update_teeth_test.go @ integration | mutation red-proved 2026-08-14: rename probe flipped to a permitted UPDATE, watched FAIL, restored | 128cb7987831 |
| RG4 | The DB caps live site_admins at one and restricts System-org membership to site_admin. | go-test | internal/postgres/model_update_teeth_test.go @ integration | mutation red-proved 2026-08-14: expected constraint name broken, watched FAIL, restored | 1e479e82a585 |
| LEASE-1 | Exactly one instance serves at a time via the DB singleton lease; a lease-less instance answers 503. | go-test | internal/lease/coordinator_test.go @ unit | mutation red-proved 2026-08-14: Acquire expectation inverted, watched FAIL, restored | aec9f1a0ec95 |
| DOMAIN-UNIQUE-1 | Organization domains are globally unique with at most one primary per organization. | go-test | internal/postgres/client_auth_boundary_test.go @ integration | mutation red-proved 2026-08-14: expected index name broken, watched FAIL, restored | b19efa711822 |
| PKJWT-ALGS-1 | OSS private_key_jwt accepts only the documented assertion algorithms bound to client and audience. | go-test | internal/service/client_assertion_validator_test.go @ unit | mutation red-proved 2026-08-14: errors.Is inverted, watched FAIL, restored | 837e1112a039 |
| ORG-DELETE-REVOKE-1 | Deleting an organization revokes its credentials at delete time. | go-test | internal/postgres/client_auth_boundary_test.go @ integration | 2026-08-18 mutated GetClientByClientID query: org-liveness EXISTS predicate 'o.deleted_at IS NULL AND o.active' -> TRUE; TestOrgDeleteEndsClientResolution FAIL 'a deleted organization''s client still RESOLVED'; source restored byte-identical, green | a4425c37d99d |
| SOFTDEL-RESOLVE-1 | A soft-deleted client or identity provider never resolves, even when its organization is live. | go-test | internal/postgres/client_auth_boundary_test.go @ integration | 2026-08-18 mutated GetClientByClientID query: client tombstone predicate 'oc.deleted_at IS NULL' -> TRUE; TestSoftDeletedClientDoesNotResolve FAIL 'a soft-deleted client RESOLVED at auth time'; source restored byte-identical, green | 3a006fadf036 |
| ORG-ADMIN-STATE-1 | The organizations API's admin state reflects the live org_admin counts; an unwired counter is a fatal fault with the fields absent, never false. | go-test | internal/handlers/organizations_admin_state_test.go @ unit | red-proved 2026-08-18: pre-emission build watched FAIL (is_claimed ABSENT from payload) on list, get, and org_admin single-row; emission added, watched PASS | fa40b8abc0e5 |
| WIRE-CONTRACT-ORG-1 | The org read surface's JSON key set (always + omitempty, with the tri-state admin pair absent-by-default) matches the pinned wire contract exactly. | go-test | internal/handlers/organizations_wire_contract_test.go @ unit | mutation red-proved 2026-08-18: is_claimed json tag flipped to was_claimed, watched FAIL (omitempty key set drifted), restored byte-identical, watched PASS | bcf994289cbf |
| TEST-DB-GUARD-1 | The DB-backed test harness refuses any DSN whose database name does not end in _test, unless the disposable-DB escape is set. | go-test | internal/testsupport/dbguard_test.go @ unit | 2026-08-19 mutated internal/testsupport/dbguard.go RequireTestDatabase: suffix check short-circuited to always-true so a non-_test DSN was accepted; TestRequireTestDatabaseRefusesNonTestDSN FAIL 'dev DSN (identuum_idp_oss) must be REFUSED'; reverted byte-identical, green | 8942f85de514 |
| SIGNING-KEY-SEAL-1 | An undecryptable active signing key is a health-visible NOT-SERVING fault, not a silent log line under a green health. | go-test | internal/runtime/signing_key_seal_test.go @ unit | 2026-08-19 mutated internal/runtime/signing_key_seal.go signingKeySealFault to return false so the active-but-undecryptable brick state no longer faults; TestSigningKeySealFault FAIL 'signingKeySealFault(1, 0) = false, want true'; reverted byte-identical, green | cd51205dac3d |
| WIZARD-ADOPT-1 | The setup wizard adopts-and-resets an existing site_admin to the operator's submitted password, never silently discarding it. | go-test | internal/setup/service_test.go @ unit | 2026-08-19 reverted internal/setup/service.go ensureSiteAdmin to the silent 'return nil' on an existing site_admin (dropping the adopt-and-reset Update); TestComplete_AdoptsAndResetsExistingSiteAdmin FAIL 'wizard silently discarded the operator password — the old hash survived'; restored byte-identical, green | 6356a852f425 |
| BOOTSTRAP-SETUP-COHERENT-1 | Bootstrap marks the setup-state row complete after creating the site_admin, so a bootstrapped database is coherent and cannot be a wizard split-brain. | go-test | cmd/identuum-idp/bootstrap_test.go @ unit | 2026-08-19 mutated cmd/identuum-idp/bootstrap.go finishBootstrap to return 0 before marking setup complete; TestBootstrapCore_MarksSetupComplete FAIL 'bootstrap created a site_admin but did NOT mark setup complete'; restored via edit byte-identical, green | ae17374b8d55 |
| WIZARD-COMPLETE-LOGIN-1 | The setup completion result states the pinned site_admin login identity, not the operator's typed address. | go-test | internal/setup/service_test.go @ unit | 2026-08-19 mutated internal/setup/service.go Complete: CompleteOutput.LoginEmail set to in.AdminEmail (typed address) instead of the pinned siteAdminIdentity login; TestComplete_ReportsPinnedLoginIdentity FAIL 'LoginEmail = owner@acme.example, want site_admin@system.local'; restored via edit, green | 02f45bf7a489 |
| SETUP-COHERENCE-1 | The database refuses to mark setup complete unless a live site_admin exists, so setup-state and site_admin existence cannot disagree. | go-test | internal/postgres/setup_coherence_teeth_test.go @ integration | 2026-08-19 mutated migration 0031 model_setup_complete_requires_site_admin (IF FALSE AND on the guard), re-applied live via psql; TestSetupCompleteRequiresSiteAdmin FAIL 'marking setup complete with NO site_admin SUCCEEDED'; migration restored via edit, function re-applied, green | 414c345ca307 |
| WIRE-CONTRACT-HEALTH-1 | GET /api/v1/health/details emits exactly status+version always, database/audit_system/redis omitempty, with redis and audit queue_depth absent on OSS, and leaks no DSN/key/session material. | go-test | internal/handlers/system_health_wire_contract_test.go @ unit | 2026-08-20 mutated internal/handlers/system_health.go handleSystemHealthDetails: zero-faked resp.Redis with a 'unavailable' component; TestWireContractHealth_JSONKeySetIsPinned FAIL 'redis must be ABSENT on OSS (no Redis dependency)'; restored via edit byte-identical, green | 5c7c2b5a3d47 |
| SESSION-REPLAY-THEFT-1 | A consumed refresh-token predecessor replayed after the rotation grace window is treated as theft (ErrUserSessionReuse) and revokes the whole session family. | go-test | internal/e2e/session_rotation_concurrency_pgx_test.go @ integration | 2026-08-20 mutated internal/service/user_session_service.go RotateRefreshToken benign-racer grace guard: now.Sub(*session.PrevRotatedAt) < sessionRotationGraceWindow changed to < time.Hour, so a past-grace predecessor is wrongly classified benign; TestE2E_OSS_SessionRotation_ReplayIsTheft FAIL 'replay of a consumed token past the grace window must be treated as reuse; got <nil>'; guard restored byte-identical (git diff --quiet clean), green. Test hardened THIS slice (THE-TIMING-MARGIN): grace-window age is now measured on a SINGLE clock — prev_rotated_at is backdated from backend/host time instead of an 11s host sleep against a DB-clock prev_rotated_at, so host-vs-VM drift cannot flip the verdict (was flaky at ~1s real drift). | 610bc41f51f6 |
| DSN-DEFAULT-1 | One-shot subcommands default the database URL from IDENTUUM_IDP_DATABASE_URL then IDENTUUM_IDP_OSS_DB when the positional is absent; an explicit argument wins; the URL is never printed. | go-test | cmd/identuum-idp/main_test.go @ unit | 2026-08-20 mutated cmd/identuum-idp/main.go requirePositionalURL: env-fallback slice [IDENTUUM_IDP_DATABASE_URL, IDENTUUM_IDP_OSS_DB] emptied to []string{}; TestRequirePositionalURL_EnvFallback FAIL 'want the primary env var' + 'want the compose-shaped env var'; restored via edit, green | 60f31f40a7f5 |
| DOCTOR-1 | The doctor subcommand is read-only, prints named states (version, db, at-rest-key, setup, signing-key-seal), exits 0 healthy and non-zero naming each failing state, and never prints DSNs or key material. | go-test | cmd/identuum-idp/doctor_test.go @ unit | 2026-08-20 mutated cmd/identuum-idp/doctor.go doctorCore: dropped the failing=append(failing, "signing-key-seal") on the SEALED branch so a bricked install exits 0; TestDoctorCore_NamedStatesAndExitCodes FAIL 'sealed install must exit non-zero'; restored via edit, green. Also exercised live on a disposable scratch DB: healthy exit 0 with all five named states, and under an induced wrong-at-rest-key fault exit 1 FAILING: signing-key-seal | 7725190b754d |
| FACTORY-RESET-GUARD-1 | factory-reset refuses — before contacting any database — unless the explicit --i-understand-this-destroys-all-data flag is passed. | go-test | cmd/identuum-idp/factory_reset_test.go @ unit | 2026-08-20 mutated cmd/identuum-idp/factory_reset.go dispatchFactoryReset: refusal guard neutralized (if !confirmed -> if false && !confirmed) so a flag-less run proceeds to dial; TestFactoryReset_RefusesWithoutExplicitFlag FAIL 'exit = 1, want 2' (it contacted the database); restored via edit, green. Destructive execution exercised ONLY against the disposable identuum_idp_oss_operator_scratch DB (wiped, re-migrated 31/31, dropped after); live identuum_idp_oss untouched (3 users before and after) | 10f1d58b50ff |
| TOOLCHAIN-PARITY-1 | The image builder Go version equals go.mod's go directive (digest-pinned), so host/image toolchain skew cannot ship silently. | go-test | cmd/identuum-idp/toolchain_parity_test.go @ unit | 2026-08-20 mutated deployment/Dockerfile.local: builder FROM lowered golang:1.26.6-bookworm -> golang:1.26.5-bookworm (digest untouched) while go.mod stayed 1.26.6 — the exact skew shape that shipped 8 HIGH stdlib CVEs into the v0.3.4 image; TestToolchainParity_DockerfileBuilderEqualsGoModDirective FAIL 'HOST/IMAGE TOOLCHAIN SKEW: Dockerfile builder pins golang 1.26.5 while go.mod says go 1.26.6'; restored via edit, green | 6e7d7804648c |
| CHANGE-PASSWORD-1 | Self-service change-password refuses a wrong current password, enforces the org password policy, pins its wire envelopes, and on success revokes every OTHER session and all refresh tokens while the changing session stays valid (R2 ruled 2026-08-21). | go-test | internal/handlers/auth_change_password_test.go @ unit | 2026-08-20 mutated internal/service/change_password_service.go ChangeOwnPassword: current-password verify neutralized (err != nil -> err != nil && false) so ANY current password is accepted; TestChangePassword_ContractAndRefusals FAIL 'status = 204, want 403' at the wrong-current subtest (a wrong password rotated the hash); restored via edit, green. R2 deliberately unruled: the success case structurally asserts NO session/refresh revocation via spy revokers that fail on any call | 4f76b273f770 |
| PW-REVOKE-1 | After a successful password change every OTHER session is revoked and the changing session stays valid; refresh tokens are revoked; refusals revoke nothing (R2). | go-test | internal/handlers/auth_change_password_test.go @ unit | 2026-08-21 mutated internal/handlers/auth_change_password.go: the R2 session fan-out branch skipped (if deps.UserSession != nil ... -> if false && ...); TestPWRevoke_OtherSessionsDieCurrentSurvives WATCHED the hijacked session stay ALIVE — FAIL 'R2: the OTHER pre-existing session must be revoked after a password change'; restored via edit, green. The rule also pins the keep-current side (the changing session must STAY VALID, the all-sessions seam guarded by a failing spy) and exactly-once refresh revocation | 9cb8b8a0f62d |
| AUTHZ-ORGCREATE-1 | An org_user cannot create an organization: the site_admin-only gate answers 403 and writes no row. | go-test | internal/handlers/tenant_scoped_users_test.go @ unit | 2026-08-21 mutated internal/handlers/organizations.go: the siteOnly RequireSiteAdmin middleware unmounted (commented out); TestOrganizationsCreate_OrgUserForbidden WATCHED the escalation SUCCEED — FAIL 'org_user create-organization = 201, want 403' with a real organization row minted; restored byte-identical (git diff --quiet), green. Closes manual-matrix authz.4, the one THE-REFUSAL-COVERAGE subset check no instrument witnessed directly | fd40c099700a |
| ROUTE-RATELIMIT-1 | The production router's /api/v1/oauth/token endpoint enforces the configured RateLimitConfig.TokenLimit: requests past the limit answer 429 through the REAL router built by NewOSSEngine, not merely through a synthetic-route mechanism test. | go-test | internal/api/token_ratelimit_wiring_test.go @ unit | 2026-08-21: mutated internal/api/router.go mountToken — replaced resolved.RateLimitConfig.TokenLimit with ratelimit.RateLimit{} (zero-value => noop middleware) in BOTH mounts (PreAuthLimiter oauth-token-preauth + Limiter oauth-token). TestTokenRoute_RealRouterEnforcesConfiguredLimit FAILED: request 4 exceeded TokenLimit=3 yet the production router answered 401, want 429 (codes=[401 401 401 401]). Restored byte-identical (sha256 f2d490746f32aaa7759c50f4641b4e3027024c86e0501a6e8c45eeb761d81c13); test green again. Gap this closes: RATE-TOKEN-1's check (internal/mw/rate_limit_test.go) proves the mechanism on a synthetic /probe route only — deleting either TokenLimit mount left every test green. | a27b242a82cc |
| SESSION-CACHE-REVOKE-1 | Revoking a session — singly, per user, or per organization — evicts its cached validation entry, so a revoked session never validates from the cache and the next validation consults the database. | go-test | internal/repository/cached_session_repository_test.go @ unit | mutation red-proved 2026-08-23, four mutations each WATCHED failing then restored byte-identical (git diff --quiet), suite green after: (A) Revoke's both Del calls neutralized (if false) -> FAIL 'revoked session must not validate from cache: Revoke left the cached validation entry behind' plus 'An error is expected but got nil' on the serving-path read; (B) RevokeByUserID's two Del sweeps emptied -> FAIL 'user-revoked session must not validate from cache' for s2 AND s3; (C) RevokeByOrganizationID's page eviction emptied -> FAIL 'org-revoked session must not validate from cache'; (D) escalation pinning the shared primitive: cache.RedisClient.Del no-opped -> all five assertions FAIL at once. Seeding goes THROUGH the public validation read (proven cache hit before revocation), so the check asserts what the serving path returns, not merely key existence. | 8603f18eeff5 |
| CACHE-RESOLVE-DELETE-1 | Deleting a client or an identity provider evicts every cache key it can be resolved through, so a deleted client or identity provider never resolves from the cache and the next resolve consults the database. | go-test | internal/repository/cached_resolve_after_delete_test.go @ unit | mutation red-proved 2026-08-23, three mutations each WATCHED failing then restored byte-identical (git diff --quiet), suite green after: (A) client Delete's invalidateClientCache call neutralized -> FAIL 'deleted client must not resolve from cache: GetClientByClientID served a cache remnant' AND the GetClientByID twin; (B) idp Delete's invalidation neutralized in BOTH branches -> FAIL all three idp reads: 'GetByID served a cache remnant', 'GetByOrgAndType (HRD login path) served a cache remnant', 'the org list served a cache remnant'; (C) escalation pinning the shared primitive: cache.RedisClient.Del no-opped -> all five assertions FAIL at once. Seeding goes THROUGH the public reads with proven cache hits before the delete (each delegate expectation .Once), so every assertion is about what the serving path returns, not key existence. One sentence covers both layers because ONE tagged body drives both. | da20c2c89cf1 |
| CACHE-WRITE-INVALIDATE-1 | Updating or deleting a user, organization, or service account evicts its cached entries, so a mutated or deleted entity's stale snapshot is never served and the next read consults the database. | go-test | internal/repository/cached_write_coherence_test.go @ unit | mutation red-proved 2026-08-23, four mutations each WATCHED failing then restored byte-identical (git diff --quiet), suite green after: (A) user Update+Delete invalidation neutralized -> FAIL 'updated user must not be served from cache: stale snapshot returned after Update' AND 'deleted user must not be served from cache'; (B) org Update+Delete invalidation neutralized -> FAIL the organization twins; (C) service-account Update+Delete invalidation neutralized -> FAIL the service-account twins; (D) escalation pinning the shared primitive: cache.RedisClient.Del no-opped -> ALL SIX assertions FAIL at once. Every path seeds THROUGH the public read with a proven cache hit (.Once delegate expectations); assertions are about what the serving path returns, not key existence. | aefd3b130c82 |
| SESSION-CACHE-ROTATE-1 | A successful session rotation evicts the cached validation snapshot so the next validation consults the database, and the session-by-id read is never served from a cache at all. | go-test | internal/repository/cached_write_coherence_test.go @ unit | mutation red-proved 2026-08-23, three mutations each WATCHED failing then restored byte-identical: (E) RotateToken's won-branch eviction neutralized -> FAIL 'post-rotation validation must consult the database, not a cache remnant'; (F) the forbidden regression IMPLEMENTED as the mutation — caching added to session GetByID -> FAIL 'session-by-id read must never be served from a cache: the second read did not consult the database'; (D) escalation cache.RedisClient.Del no-opped -> the rotation assertion FAILS (the never-cached clause is Del-independent by design). Validation snapshot seeded through the public read with a proven hit before rotation; the by-id clause proves BOTH consecutive reads reach the delegate. | 7799097983c2 |
| DCR-IAT-CONSUME-1 | A DCR initial access token consumes atomically within its use limit and never after revocation; every refusal — unknown, exhausted, revoked — is the one opaque invalid-token error. | go-test | internal/postgres/dcr_iat_boundary_test.go @ integration | mutation red-proved 2026-08-23 against the LIVE SQL (_test DB), each WATCHED failing then restored byte-identical: (M1) the use-limit clause removed from ConsumeByHash's atomic UPDATE predicate -> FAIL 'a single-use IAT consumed twice: the second consume must be refused with ErrIATInvalid, got <nil>'; (M2) the revoked clause removed -> FAIL 'a revoked IAT must not consume: want ErrIATInvalid, got <nil>'. The check drives the SERVICE Consume the /register handler calls (public path) over the real pgx repository; opaqueness asserted by the shared sentinel across unknown/exhausted/revoked. | a5cac688c508 |
| DCR-RAT-LIFECYCLE-1 | A registration access token authorizes RFC 7592 management only while it is the client's live token: wrong and rotated-away tokens get the one opaque 401, and after revocation the client disappears from the management surface (404, not 401 — no enumeration). | go-test | internal/handlers/dcr_rat_lifecycle_test.go @ unit | mutation red-proved 2026-08-23 through the ROUTED handlers, each WATCHED failing then restored byte-identical: (M3) dcrManagementAuthorize's bearer-check branch neutralized (false &&) -> FAIL 'a wrong registration access token must be refused with the opaque 401, got 200' AND the rotated-away twin; (M4) forbidden behavior IMPLEMENTED — service Authenticate returns nil before checking -> both refusal assertions FAIL with got 200; (M5) service Revoke made a no-op -> FAIL 'after revocation the client must disappear from the management surface (404, not 401): got 200'; (M7) escalation crypto.HashSecret no-opped to a constant -> wrong AND rotated tokens hash-collide and are ACCEPTED, both refusal assertions FAIL at once (plus the APIRES-AUTH-1 wrong-secret assertion in the same run). | 4c7a120d9b53 |
| APIRES-AUTH-1 | An API resource authenticates only by constant-time match of its active audience-bound secret; a wrong secret is refused with the one opaque credentials error. | go-test | internal/service/oauth_client_auth_service_test.go @ unit | mutation red-proved 2026-08-23, each WATCHED failing then restored byte-identical: (M6) the ConstantTimeCompare refusal neutralized (false &&) -> FAIL 'wrong secret = <nil>, want ErrInvalidAPIResourceCredentials'; (M7) escalation crypto.HashSecret no-opped to a constant -> the wrong secret hash-collides and is ACCEPTED, same assertion FAILS in the same run as the RAT-lifecycle refusals. Pre-existing hermetic test body adopted by tag; sentence matches exactly what it asserts. | 2d536c0684fc |
| AUTHORIZE-REDIRECT-EXACT-1 | The OAuth authorize endpoint issues a code redirect only to a redirect_uri byte-exact-matching one registered for the client; an unregistered or prefix-extended redirect_uri is refused with a direct 400 and never receives a redirect. | go-test | internal/handlers/authorize_redirect_exact_test.go @ unit | mutation red-proved 2026-08-23 through the routed HandleAuthorize, each WATCHED failing then restored byte-identical: (M1) forbidden behavior — IsRedirectURIAllowed exact-equality replaced with strings.HasPrefix -> FAIL 'unregistered redirect_uri "https://app.example.com/cb/../../evil" must be a direct 400, got 302' AND 'must NOT receive a redirect, got Location=…evil'; (M2) DISTINCT teeth — emitAuthorizeError's ErrAuthorizeInvalidRedirectURI branch rerouted from a direct 400 to redirectAuthorizeError -> FAIL 'unregistered redirect_uri "https://evil.example.com/cb" must be a direct 400, got 302' with Location to the attacker host. M1 pins the exact-match, M2 pins the 400-not-302 routing; neither implies the other. | 8766bf07cd9c |
| OPEN-REDIRECT-RELATIVE-1 | A user-supplied return target is honored only as a same-site relative path; any absolute, protocol-relative, scheme/host-bearing, or CR/LF-bearing value is refused so the login flow cannot become an open redirect. | go-test | internal/handlers/open_redirect_return_to_test.go @ unit | mutation red-proved 2026-08-23, WATCHED then restored byte-identical: (M3) forbidden behavior — validateReturnTo's protocol-relative guard (HasPrefix '//' -> '') removed -> FAIL 'off-origin return target "//evil.example.com" must be refused, got "//evil.example.com"'. The check asserts the sanitizer's returned value (what the serving path would redirect to). Same-package twin sanitizeReturnURL (service) is the byte-identical guard, carried by reasoning in the census reconciliation, NOT by a coverage edge. | bbac343ae7f1 |
| UPSTREAM-URL-HTTPS-1 | An upstream federation URL (OIDC issuer, JWKS URI) is accepted only when absolute with an https scheme (http solely under an explicit plain-HTTP opt-in) and a non-empty host, before any fetch leaves the server. | go-test | internal/service/upstream_url_https_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 (re-red-proved after the parseAndValidate extension), each WATCHED then restored byte-identical. (M4) the service validateIssuerURL https requirement dropped (host-only kept) so plaintext-http and ftp issuers are admitted -> FAIL 'issuer http://idp.example.com must be refused, got nil' plus the ftp and validateOIDCInput http cases. (M5) DISTINCT — the ClientJWKSFetcher validateJWKSURI scheme switch collapsed to accept-all -> FAIL 'jwks_uri http://jwks.example.com/keys must be refused before fetch' plus ws. (M6-NEW) parseAndValidate is now reached EXACT depth 1 by a direct call, no longer carried by reasoning: neutralizing its issuer-consistency check admits an issuer mismatch -> FAIL 'an issuer mismatch must be refused'; its plaintext-http endpoint refusal is pinned via isAllowedURL. The domain twins validateIssuerURL/validateJWKSURL are pinned EXACT one package over by TestValidateIssuerURL_Direct and TestValidateJWKSURL_Direct. | db407b844f17 |
| SESSION-SELF-REVOKE-1 | A session-revocation endpoint acts only on the authenticated principal's own sessions: it exposes no cross-user target and a session owned by another user is never revoked (the request gets the same opaque success as a real revoke, but the victim's session stays live). | go-test | internal/handlers/session_self_revoke_test.go @ unit | mutation red-proved 2026-08-23 through the routed HandleRevokeOwnSession, WATCHED then restored byte-identical: (M1) forbidden behavior — the ownership guard 'sess.UserID != principal.UserID' removed from the sess-nil check -> FAIL 'cross-user revocation: another user's session <id> was revoked' (the revoker was invoked for a victim session owned by a different user). The check authenticates as user A, POSTs a session id owned by user B, and asserts the revoker is NEVER called for B's id while still being called for A's own session; assertions are which id reached the fakeSessionRevoker, not internal state. | 62aa65369366 |
| REDIRECT-URI-SCHEME-1 | A redirect_uri is refused at registration when its scheme is a dangerous script/data scheme (javascript, data, vbscript, file) or is not a well-formed RFC 3986 scheme, so a dangerous scheme can never enter a client's allowlist. | go-test | internal/domain/redirect_uri_scheme_test.go @ unit | mutation red-proved 2026-08-23, WATCHED then restored byte-identical: (M2) forbidden behavior — ValidateRedirectURIs' dangerous-scheme denylist loop neutralized (false &&) -> FAIL 'redirect_uri "javascript:alert(1)" must be refused at registration, got nil' AND the case-insensitive 'JavaScript:...' variant. Distinct from the authorize-time exact match (AUTHORIZE-REDIRECT-EXACT-1): this is the registration-time scheme gate keeping dangerous schemes out of the allowlist. | 22914e1a6308 |
| LOGOUT-URI-GUARD-1 | A per-client backchannel logout URI is accepted only when absolute (has a host) and free of a #fragment, and when https is required uses https, before the server POSTs a logout token to it; empty is allowed. | go-test | internal/service/logout_uri_guard_test.go @ unit | mutation red-proved 2026-08-23, WATCHED then restored byte-identical: (M3) forbidden behavior — validateLogoutURI's host-empty and fragment guards neutralized -> FAIL 'logout URI "https://" without a host must be refused, got <nil>' AND 'logout URI with a fragment must be refused, got <nil>'. The check asserts the returned sentinel for the shapes a backchannel POST must never reach. | 8ea435498db4 |
| ATREST-CIPHER-1 | Every secret the crypto service stores is AES-GCM ciphertext under the active key that round-trips through Decrypt; a key that did not seal a ciphertext fails closed (an unknown v2 key id is refused, and a wrong key never opens an unkeyed ciphertext) and plaintext is never the stored form. | go-test | internal/crypto/atrest_cipher_test.go @ unit | mutation red-proved 2026-08-24, WATCHED then restored byte-identical: forbidden behavior — Encrypt made to return the plaintext instead of the v2 AES-GCM ciphertext -> FAIL 'stored form must be a v2 ciphertext, got prefix "mfa"'. The check also asserts a foreign key cannot open a v2 or v1 ciphertext and an unknown v2 key id is refused; the mutation carries the at-rest-is-ciphertext assertion. | a89eaf8b274d |
| ATREST-KEY-RESOLVE-1 | The at-rest encryption key is resolved by the documented precedence (operator env, then the persisted 0600 key file, then a freshly generated 0600 file) and an empty key file is refused rather than silently regenerated, which would orphan every already-encrypted secret; the resolved key is written only into Config. | go-test | internal/appliance/encryption_key_resolve_test.go @ unit | mutation red-proved 2026-08-24, WATCHED then restored byte-identical: forbidden behavior — ResolveEncryptionKey's empty-key-file branch made to fall through to generation instead of refusing -> FAIL 'an empty key file must be refused, not silently regenerated'. The check also pins operator-env precedence, persisted reuse, and generated-file 0600 mode; the mutation carries the refuse-empty (no-silent-orphan) assertion. | 0baec4e267c6 |
| SIGNING-KEY-ATREST-1 | Signing-key private material is protected at rest: legacy plaintext PEM rows are re-encrypted in place to v2 AES-GCM ciphertext that still decrypts to the identical PEM, and the sweep is idempotent — plaintext private keys are actively purged, never left at rest. | go-test | internal/postgres/key_repository_pgx_encryption_test.go @ integration | mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: ReEncryptPlaintextKeys' encrypt+UPDATE loop body neutralized (row counted but not written) -> FAIL 'swept row not v2 ciphertext: prefix "----"' (the legacy plaintext PEM stayed plaintext at rest). The check also reads the row back through decryptPrivateKey (got.PrivateKey == the original PEM) and asserts a second sweep is idempotent. | c58aaba3489a |
| SIGNING-KEY-ROTATE-1 | Signing-key rotation is atomic through the service Rotate: the old active key becomes rotating (still validates, no longer the primary signer) and the new key becomes active in ONE transaction, and a rotation that cannot complete rolls back so the old key is never stranded in rotating. | go-test | internal/postgres/key_rotation_integration_test.go @ integration | mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: RotateSigningKey step 1 (mark old key rotating) moved OFF the transaction onto r.db so it escapes rollback -> FAIL 'after a failed rotation the old key state = "rotating", want active (transaction must roll back)'. The happy path (old->rotating, new->active) is asserted in the same test; the mutation carries the atomicity/rollback assertion. | cda4901c7f35 |
| SESSION-USABLE-1 | Session.CanBeUsed gates every disqualifier: an invalid, revoked, or expired session is not usable, and a revoked session surfaces its recorded reason; only a valid, unrevoked, unexpired session is usable. | go-test | internal/domain/session_usable_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: CanBeUsed's revoked branch guard changed to 's.RevokedAt != nil && false' so a revoked session reports usable -> FAIL 'a revoked session must not be usable and must surface its reason: ok=true reason=""'. The check also proves the invalid and expired disqualifiers and that a valid/unrevoked/unexpired session is usable. | 1152dc557133 |
| CSRF-VERIFY-1 | BrowserCSRFService.Verify rejects a token whose HMAC tag does not verify under the service secret with ErrBrowserCSRFInvalid — a forged tag can never pass the double-submit check. | go-test | internal/service/browser_csrf_service_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the constant-time HMAC-tag comparison in Verify neutralized ('!= 1 && false') so a forged tag is accepted -> FAIL 'err = <nil>' where errors.Is(err, ErrBrowserCSRFInvalid) was required for a hand-crafted forged token. | 8be925d81f7a |
| VALIDATE-COOKIE-1 | GET /api/v1/validate trusts the access_token cookie (preferred over the Authorization header) only after the token verifies to a principal whose session is usable and whose user is neither banned nor deleted; any break in that chain is a 401. | go-test | internal/handlers/validate_cookie_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the session-usable gate in HandleValidateSession neutralized ('!ok && false') so a revoked-session principal is trusted -> FAIL 'a revoked session must be 401, got 200'. The check also proves cookie-preferred-over-header, no-token 401, unverifiable 401, banned-user 401, and the 200 full-chain path. | 6fe980e4e945 |
| BROWSER-SESSION-TOKEN-1 | The browser-session wire token is never handed to the repository in plaintext: BrowserSessionTokenService.Revoke passes the token's SHA-256 hex digest, and an empty wire token is a no-op. | go-test | internal/service/browser_session_token_hash_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: Revoke changed to pass the raw wireToken instead of hashBrowserSessionToken(wireToken) -> FAIL 'the PLAINTEXT wire token reached the repository — it must be hashed first'. The check also pins the digest is sha256 hex (len 64) and that an empty wire token reaches the repo zero times. | 75432e8f1931 |
| BROWSER-SESSION-REVOKE-1 | The pgx browser-session-token sinks revoke by token hash and by session id: each flips revoked_at on the matching active row and is idempotent — a second revoke leaves the original revoked_at untouched. | go-test | internal/postgres/browser_session_revoke_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: RevokeByTokenHash's UPDATE WHERE clause neutralized ('AND false') so no row is written -> FAIL 'RevokeByTokenHash did not set revoked_at'. The check also proves RevokeByTokenHash idempotency and RevokeBySessionID flips revoked_at on the session's row. | 77bbc3ce2931 |
| SESSION-SINK-SCOPE-1 | The pgx session-revocation sinks flip revoked_at strictly inside their WHERE predicate — Revoke touches only the one session id, RevokeByUserID touches only that user's active sessions — so a sibling session or another user's session is never revoked, and the revoked_at IS NULL guard makes a repeat call leave the existing revoked_at untouched. | go-test | internal/postgres/session_revoke_scope_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: (M1) Revoke's 'WHERE id = $1' broadened to '(id = $1 OR true)' -> FAIL 'Revoke(a1) must not touch the sibling session a2' (and another user's b1); (M2) RevokeByUserID's 'WHERE user_id = $1' broadened to '(user_id = $1 OR true)' -> FAIL 'RevokeByUserID(userA) must not touch another user's session b1'; (M3) Revoke's 'AND revoked_at IS NULL' idempotency guard dropped -> FAIL 'a repeat Revoke ... must be ErrSessionNotFound, got <nil>' plus revoked_at moved. Seeds two users x three sessions and reads revoked_at back only on the targeted rows. | feaa52f9eeeb |
| SESSION-SIBLING-PRINCIPAL-1 | The self-service sibling session-revocation endpoints (revoke-all, revoke-current, revoke-others) derive the acting identity only from the authenticated principal — a user_id query or a request body is refused 400, never used as a target — and act only on the principal's own sessions: another user's session is never revoked and revoke-others preserves the current session. | go-test | internal/handlers/sibling_self_revoke_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M-a) revoke-all's user_id-query/body 400 guard neutralized -> FAIL 'a user_id query on revoke-all must be 400, got 204'; (M-b) revoke-others' 'session.UserID != principal.UserID' skip neutralized -> FAIL 'revoke-others must never revoke another user's session'; (M-c) revoke-others' 'session.ID == principal.SessionID' skip neutralized -> FAIL 'revoke-others must PRESERVE the current session'. Driven through RegisterAuthSessionRoutes over a real UserSessionService on a spy repo; all three handlers reached EXACT depth 2. | 5f2c4cdf2111 |
| USER-LIFECYCLE-CASCADE-1 | cascadeRevokeUser revokes the target user's sessions AND all their OAuth refresh tokens — both scoped to exactly that user id — and is best-effort: a revoker error is logged, not propagated, and a nil revoker is skipped. | go-test | internal/handlers/lifecycle_cascade_scope_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) session guard 'if sessions != nil && false' -> FAIL 'session cascade must revoke exactly the target user, got []'; (M2) refresh guard 'if refresh != nil && false' -> FAIL 'refresh cascade must revoke exactly the target user, got []'; (M3) an early return after a session-revoke error -> FAIL 'best-effort: refresh revoke must still fire after a session-revoker error, got 0 calls'. Calls cascadeRevokeUser directly over spy revokers; exact depth 1. | bd7b5369706f |
| CONSENT-REVOKE-1 | The pgx OAuth-consent Revoke sink flips revoked_at only on the row matching (user_id, client_id, audience) — another user's or client's consent stays live — and is idempotent (a repeat leaves the existing revoked_at untouched). | go-test | internal/postgres/oauth_consent_revoke_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: (M1) 'user_id = $1' broadened to '(user_id = $1 OR true)' -> FAIL 'must not touch another user's consent for the same client'; (M2) 'client_id = $2' broadened -> FAIL 'must not touch the same user's consent for another client'; (M3) 'AND revoked_at IS NULL' dropped -> FAIL 'Revoke not idempotent: revoked_at moved'. Seeds (userA,X),(userA,Y),(userB,X); reads revoked_at back only on the targeted row. | 9d1f29c8a0e9 |
| USERINFO-BEARER-CHALLENGE-1 | GET/POST /api/v1/oidc/userinfo rejects a request with no valid bearer token with 401 and the RFC 6750 challenge header WWW-Authenticate: Bearer error=invalid_token, never leaking claims. | go-test | internal/handlers/userinfo_challenge_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) respondUserinfoUnauthorized status 401->200 -> FAIL 'missing bearer must be 401'; (M2) the WWW-Authenticate header line removed -> FAIL 'missing bearer must carry the RFC 6750 challenge'. Driven through the routed HandleUserinfo; respondUserinfoUnauthorized reached exact depth 3. | da226215edf4 |
| REVOKE-CLIENT-BINDING-1 | POST /api/v1/oauth/revoke fans out subject-session revocation ONLY when the authenticated OAuth client owns the token (RFC 7009 section 2.1) — a cross-client caller gets the same opaque 200 but no session is revoked. | go-test | internal/handlers/revocation_client_binding_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: sessionFanoutAllowed's ownership check (tokenClientID == authedClientID) forced always-true with a disjunctive OR-true -> FAIL 'a cross-client revoke must NEVER fan out session revocation, but it revoked [subject]'. Driven through the routed HandleRevoke over a fake client authenticator plus a real IntrospectionService and RefreshTokenService on fake seams plus a spy SessionRevoker; HandleRevoke, tryRevokeRefreshToken and authenticatedOAuthClientID reached exact depth 2 to 4. | 526046e49996 |
| CLAIM-CONSUME-ONCE-1 | An organization-claim link is consumable exactly once: GET /claim/validate is read-only (a consume right after a validate still succeeds), and POST /claim burns-before-write so a replay after a successful consume mints no second org_admin and reports failure. | go-test | internal/handlers/claim_consume_once_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) ValidateClaim made to consume ('s.claims.Delete' after the token lookup) -> FAIL 'consume after validate must succeed (validate must be read-only)'; (M2) the ConsumeClaim burn 's.claims.Delete(...)' neutralized ('if err := error(nil)') -> FAIL 'a replayed claim must not consume twice' + 'Create ran 2 times'. Driven through the routed HandleValidateClaim/HandleConsumeClaim over a real ClaimService + single-claim fake repo; all four symbols reached exact depth 2-3. | 03f21f99eda4 |
| ACTIVATION-CONSUME-ONCE-1 | Organization activation is single-use and atomic: ConsumeActivationToken claims the activation token (nulls activation_token_hash) in the SAME transaction that flips the org active, so a spent token can never be replayed; ValidateActivationToken is read-only and never consumes. | go-test | internal/postgres/activation_consume_once_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: (M1) the pgx atomic claim 'SET activation_token_hash = NULL' removed -> FAIL 'activation_token_hash must be NULL after consume'; (M2) ValidateActivationToken made to consume the token -> FAIL 'consume after validate must succeed (validate must be read-only)'. Driven through the real OrganizationActivationService over the pgx user+org repos; reaches service ValidateActivationToken/ConsumeActivationToken + pgx ConsumeActivationToken (integration ~). | c1cf2d6fac27 |
| PKCE-VERIFY-1 | pkce.Verify (S256, RFC 7636) returns true only when base64url(SHA-256(code_verifier)) equals the stored code_challenge in constant time; a mismatched or empty verifier/challenge is false. | go-test | pkg/pkce/pkce_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the constant-time compare forced always-true (a disjunctive OR-true appended) -> FAIL 'Verify(...) = true, want false' on the wrong-verifier and tampered-challenge rows. Adopted the existing table test (RFC 7636 4.6 vector plus mismatch/empty edges); Verify reached exact depth 1. | 07b54362a918 |
| IDTOKEN-HINT-VERIFY-1 | The /end_session id_token_hint is accepted only when its signature verifies under an active local signing key and its issuer equals our issuer — a foreign-key signature is ErrIDTokenHintSignature and a wrong issuer is ErrIDTokenHintIssuerMismatch. | go-test | internal/service/id_token_hint_verify_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) the issuer check 'iss != v.issuer' neutralized ('&& false') -> FAIL 'a wrong-issuer id_token_hint must be ErrIDTokenHintIssuerMismatch, got <nil>'; (M2) the parse-error return neutralized so a foreign-key-signed token is accepted -> FAIL 'a foreign-key-signed id_token_hint must be ErrIDTokenHintSignature, got <nil>'. Reuses the id_token_verifier test harness; Verify reached exact depth 1. | 30dd4b9f8a87 |
| CALLBACK-IDTOKEN-VERIFY-1 | The upstream OIDC callback ID token is accepted only when its signature verifies under the provider JWKS and its issuer, audience and nonce all match the expected values — any of a wrong issuer, an audience not containing us, or a mismatched nonce is ErrCallbackValidationFailed. | go-test | internal/service/callback_idtoken_verify_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) issuer check '&& false' -> FAIL 'a wrong-issuer upstream ID token must fail validation'; (M2) audience check '&& false' -> FAIL 'an ID token whose audience does not contain us must fail validation'; (M3) nonce check '&& false' -> FAIL 'a nonce-mismatched ID token must fail validation'. validateIDToken driven on a struct whose discovery resolves an Ed25519 key from a fake JWKS RoundTripper; reached exact depth 1. | cb2ae4da3c17 |
| DOMAIN-DNS-VERIFY-1 | A tenant domain is marked verified only when a live DNS TXT challenge record carries a token whose SHA-256 hex equals the row's stored verification-token hash — an absent record is record-not-found and a wrong token is mismatch, and in neither case is the row flipped to verified. | go-test | internal/handlers/verify_organization_domain_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: (M1) OrganizationDomainService.Verify ignores the verifier error ('err != nil && false') so a domain with no DNS record is marked verified -> FAIL 'an absent challenge record must be 400 (not verified), got 200'; (M2) the DNS verifier hash compare forced always-accept (an OR-true on 'got == expected') so a wrong TXT token verifies -> FAIL 'a wrong-token challenge record must be 400 (mismatch), got 200'. Routed HandleVerifyOrganizationDomain over a real service, real DNS verifier and fake resolver/repo; handler d1, service d2, and the DNS verifier d1 (direct call) all reached exact. | 059402578fbc |
| MFA-PENDING-CONSUME-1 | The pgx MarkConsumed sink atomically consumes a pending-MFA login handle exactly once — only a live (unconsumed, unexpired) handle is consumed, and a second consume or an expired handle is a no-op. | go-test | internal/postgres/mfa_pending_rule_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: MarkConsumed's 'AND consumed_at IS NULL' guard dropped -> FAIL 'a second MarkConsumed must be (false, nil), got (true, ...)'. Seeds a live + an expired handle; asserts single consume, no-op replay, expired no-op. | 584eb3d6907a |
| MFA-PENDING-ATTEMPTS-1 | The pgx RecordFailedVerifyAttempt sink bumps failed_attempts and, at the max-attempts threshold, invalidates the pending-MFA handle in ONE atomic statement so brute-force guessing is bounded; a non-positive threshold fails closed. | go-test | internal/postgres/mfa_pending_rule_integration_test.go @ integration | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24 against the live _test DB, WATCHED then restored byte-identical: the invalidation CASE neutralized (consumed_at = consumed_at) so the threshold never invalidates -> FAIL 'the max-attempt failure must invalidate (true)'. Also asserts below-threshold non-invalidation and maxAttempts<1 fail-closed. | 51d00a00bc9c |
| MFA-STEPUP-PASSWORD-1 | verifyCurrentPasswordForStepUp accepts a step-up proof only when the user is a local-auth account whose stored password verifies against the supplied password — a wrong password, a federated user, an empty password, or a hash-less account is refused. | go-test | internal/service/mfa_stepup_password_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the VerifyPassword check neutralized -> FAIL 'a wrong current password must fail step-up' (the federated-user and empty-password refusals are also asserted). verifyCurrentPasswordForStepUp reached exact depth 1. | de4258b5ed2a |
| WEBAUTHN-UNAUTH-1 | rejectUnauthenticated, the webauthn in-handler guard for a missing principal, aborts with 401 unauthorized. | go-test | internal/handlers/webauthn_unauth_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: rejectUnauthenticated status 401 changed to 200 -> FAIL 'rejectUnauthenticated must emit 401, got 200'. Reached exact depth 1 via a direct call. | 91c4ce9465b5 |
| WEBAUTHN-CONSUME-1 | InMemoryWebAuthnSessionRepository.Consume is atomic single-use: it deletes the entry under the same lock before returning, so concurrent consumers of one key yield exactly one winner and a replay finds nothing. | go-test | internal/repository/inmemory_webauthn_session_repository_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the atomic 'delete(r.sessions, key)' removed from Consume so it is a non-atomic get-then-nothing -> FAIL the concurrent single-use test reports more than one winner. Consume reached exact depth 1. | a65b53634036 |
| SA-ADMIN-SCOPE-1 | requireOrgAdmin authorizes a service-account admin action to site_admin or the org_admin of the SAME organization only; a cross-org org_admin gets not-found (never a 403 that leaks org existence) and an org_user is forbidden. | go-test | internal/service/sa_admin_binding_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: requireOrgAdmin's same-org check forced always-true (an OR-true) so a cross-org org_admin is admitted -> FAIL 'a cross-org org_admin must be NOT-FOUND'. Reached exact depth 1. | 46e8d22880d3 |
| SA-BINDING-ORG-1 | ValidateBindingForClient permits binding a service account to a client only when the SA is in the client's own organization and is active and unexpired — cross-org is OrgMismatch, inactive is Inactive, expired is Expired. | go-test | internal/service/sa_admin_binding_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: ValidateBindingForClient's org-match check neutralized ('&& false') so a cross-org SA binds -> FAIL 'a cross-org binding must be ErrServiceAccountOrgMismatch' (inactive + expired refusals also asserted). Reached exact depth 1. | 20a16bcd9db7 |
| SA-TENANT-IDP-1 | denySiteAdminTenantIDP refuses a site_admin principal with 403 from managing a tenant's identity providers (infrastructure authority is not a tenant super-admin); an org_admin is not denied. | go-test | internal/handlers/deny_siteadmin_idp_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: denySiteAdminTenantIDP's IsSiteAdmin gate neutralized ('&& false') so site_admin is not denied -> FAIL 'site_admin must be denied tenant IDP management (403)'. Reached exact depth 1. | 7950a7c2028d |
| SETUP-VERIFY-1 | POST /api/setup/verify-token maps the setup-token verdict onto the wire — valid token 204, invalid token 401, already-complete 410 — so an invalid token is never accepted as valid. | go-test | internal/api/setup_verify_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the ErrTokenInvalid->401 mapping changed to 204 so an invalid token is accepted -> FAIL 'an invalid setup token must be 401, got 204'. Driven through the routed RegisterSetupRoutes; handleSetupVerifyToken reached exact depth 2. | f966e74f1715 |
| AUDIT-MEMORY-1 | audit.Recorder.Record appends every event to its in-memory slice with a defensive metadata copy, capturing them in record order — the recording path never drops. | go-test | internal/audit/audit_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: Recorder.Record's 'r.events = append(...)' replaced with a no-op so events are dropped -> FAIL the capture-in-order test. Recorder.Record reached exact depth 1. | 4cfec113c2c9 |
| AUDIT-NOOP-1 | audit.NoopService.Record is the definitional no-op default: it discards the event and always returns nil. | go-test | internal/audit/audit_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: NoopService.Record changed to return context.Canceled -> FAIL 'NoopService.Record(zero) = context canceled, want nil'. Reached exact depth 1 via a direct concrete call added to the adopted test. | e89343c15999 |
| AUDIT-PERSIST-1 | PersistentAuditService.Record maps the event and inserts it via the repository, preserving its values and metadata — the recording path persists every event. | go-test | internal/service/audit_persistent_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: PersistentAuditService.Record's 's.repo.Insert(...)' replaced with 'return nil' so the event is never persisted -> FAIL the preserves-values-and-metadata test. Reached exact depth 1. | 498a51ce9b43 |
| AUDIT-ACTOR-1 | enrichActor stamps the acting principal (id, type=user, email, role) onto an audit event whose actor fields are unset, and never overwrites actor fields already set. | go-test | internal/handlers/audit_actor_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: enrichActor's ActorID stamp neutralized ('&& false') so the actor is not stamped -> FAIL 'enrichActor must stamp the principal onto an empty actor'. Reached exact depth 1. | 83e5a72aa3ad |
| NOTIFY-REDACT-1 | maskEmail redacts an email local-part before it reaches a log or notifier — it emits at most the first character followed by stars, and star-triple when there is no usable local part. | go-test | internal/service/notify_redact_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: maskEmail's masking replaced with 'return local + email[at:]' so the full local part is emitted -> FAIL 'maskEmail(alice@example.com) = alice@example.com, want a****@example.com'. Reached exact depth 1. | 7b9d61a548e0 |
| WIRE-CONTRACT-USER-1 | toSafeUser is the user wire-contract projection: it carries only the safe fields and never the password hash, MFA secret, MFA recovery codes, or activation/verification token hashes. | go-test | internal/handlers/safe_user_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: toSafeUser's 'Name: u.Name' changed to leak the password hash ('Name: &u.PasswordHash') -> FAIL 'safeUser leaked secret material'. Reached exact depth 1. | 91c4a0dccc59 |
| LOCAL-CREDENTIAL-FLOW-1 | Under an org auth_policy of idp_only, local credential login is sealed FAIL-CLOSED for everyone except the site_admin and org_admin roles: an org_user, a nil user, and any unknown or empty role are denied with ErrLocalLoginDisabledForRole as a non-nil SealReason, while a non-idp_only policy lets every role through and no allow path carries a SealReason. | go-test | internal/service/local_credential_flow_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical, TWO mutations. (A) the org_user branch flipped to Allowed true so a sealed org_user is admitted under idp_only -> FAIL 'policy=idp_only role=org_user: Allowed=true, want false' (and SealReasonNeverNilOnDeny/org_user). (B) the fail-closed default branch flipped to Allowed true so an unknown/empty role is admitted -> FAIL 'policy=idp_only role=future_role: Allowed=true, want false' plus the empty-role case (and SealReasonNeverNilOnDeny/unknown_role); the fail-closed default is the point of the function. TestIsLocalCredentialFlowAllowed_Matrix reaches IsLocalCredentialFlowAllowed exact depth 1. | e6959676220f |
| REFRESH-METADATA-SANITIZE-1 | At refresh-token issue, only the allowlisted metadata keys (client_id, client_kind, reason, grant_type) are persisted and any string value bearing a CR or LF is dropped, so a non-allowlisted key or an injection-bearing value never reaches a stored refresh token; an empty result collapses to nil. | go-test | internal/service/refresh_metadata_sanitize_rule_test.go @ unit | [proof-v1 kind=mutation_observation] mutation red-proved 2026-08-24, WATCHED then restored byte-identical: the CR/LF guard was neutralized by changing the checked character set from carriage-return-plus-newline to the empty set so it never matches, letting an injection-bearing value survive -> FAIL 'a CR/LF-bearing value must be dropped, got map[reason:ok...]'. The same test also pins the key allowlist (a non-allowlisted key is dropped) and the empty-to-nil collapse. Reaches sanitizeRefreshMetadata exact depth 1. | 3d6088c48561 |
