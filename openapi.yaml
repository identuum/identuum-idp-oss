# Generated by identuum-idp-oss/tools/api-docgen (--format=openapi).
# Do not hand-edit. To update, edit the // docgen: block above the relevant route registration and re-run the generator.
openapi: "3.0.3"
info:
  title: "identuum-idp-oss API"
  version: "identuum-idp-oss-api-docgen 0.2.0-p3"
  description: "Machine-generated OpenAPI 3 specification for the identuum-idp-oss API surface. Generated by tools/api-docgen from // docgen: source annotations. Response bodies without a known schema record are described but not field-modeled; see the referenced Go symbol in identuum-idp-oss for the authoritative shape."
paths:
  "/.well-known/jwks.json":
    get:
      operationId: "identuum-idp-oss.top-level.jwks_handler"
      summary: "Public JWKS (signing key public material only — never the private half)."
      tags:
        - "top-level"
      security: []
      responses:
        "200":
          description: "Public JWKS (signing key public material only — never the private half)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/.well-known/openid-configuration":
    get:
      operationId: "identuum-idp-oss.top-level.discovery_handler"
      summary: "OIDC discovery document."
      tags:
        - "top-level"
      security: []
      responses:
        "200":
          description: "OIDC discovery document."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/setup/complete":
    post:
      operationId: "identuum-idp-oss.setup.handle_setup_complete"
      summary: "Complete first-run setup — creates the first organization, site_admin, and signing key."
      tags:
        - "setup"
      security: []
      responses:
        "200":
          description: "Complete first-run setup — creates the first organization, site_admin, and signing key."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/setup/status":
    get:
      operationId: "identuum-idp-oss.setup.handle_setup_status"
      summary: "First-run setup status — no-secrets snapshot of setup_required/setup_complete."
      tags:
        - "setup"
      security: []
      responses:
        "200":
          description: "First-run setup status — no-secrets snapshot of setup_required/setup_complete."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/setup/verify-token":
    post:
      operationId: "identuum-idp-oss.setup.handle_setup_verify_token"
      summary: "Verify a candidate setup token plaintext against the stored hash."
      tags:
        - "setup"
      security: []
      responses:
        "204":
          description: "Verify a candidate setup token plaintext against the stored hash."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/admin/backchannel-logout-deliveries":
    get:
      operationId: "identuum-idp-oss.admin.list_backchannel_deliveries"
      summary: "List back-channel logout delivery audit rows (safe projection — never exposes the raw logout_token bytes)."
      tags:
        - "admin"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List back-channel logout delivery audit rows (safe projection — never exposes the raw logout_token bytes)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeBackchannelDelivery"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/admin/backchannel-logout-deliveries/{id}":
    get:
      operationId: "identuum-idp-oss.admin.get_backchannel_delivery"
      summary: "Show a single back-channel logout delivery row (safe projection)."
      tags:
        - "admin"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single back-channel logout delivery row (safe projection)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeBackchannelDelivery"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/admin/backchannel-logout-deliveries/{id}/replay":
    post:
      operationId: "identuum-idp-oss.admin.replay_backchannel_delivery"
      summary: "Replay a back-channel logout delivery (re-mints a fresh logout_token; the raw token is never returned to the caller)."
      tags:
        - "admin"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Replay a back-channel logout delivery (re-mints a fresh logout_token; the raw token is never returned to the caller)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/api-resources":
    get:
      operationId: "identuum-idp-oss.api-resources.list_api_resources"
      summary: "List API resources (Authorization Server feature)."
      tags:
        - "api-resources"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List API resources (Authorization Server feature)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeAPIResource"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.api-resources.create_api_resource"
      summary: "Create an API resource."
      tags:
        - "api-resources"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create an API resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeAPIResource"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/api-resources/{id}":
    get:
      operationId: "identuum-idp-oss.api-resources.get_api_resource"
      summary: "Show a single API resource."
      tags:
        - "api-resources"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single API resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeAPIResource"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.api-resources.update_api_resource"
      summary: "Update an API resource."
      tags:
        - "api-resources"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update an API resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeAPIResource"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.api-resources.delete_api_resource"
      summary: "Delete an API resource."
      tags:
        - "api-resources"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Delete an API resource."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/api-resources/{id}/secret/regenerate":
    post:
      operationId: "identuum-idp-oss.api-resources.regenerate_api_resource_secret"
      summary: "Rotate an API resource secret (new secret returned ONCE; never stored or echoed elsewhere)."
      tags:
        - "api-resources"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Rotate an API resource secret (new secret returned ONCE; never stored or echoed elsewhere)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/audit/events":
    get:
      operationId: "identuum-idp-oss.audit.list_audit_events"
      summary: "Read the OSS plain persistent audit log, newest first. Filter by event_type, outcome, actor_id, subject_id and a created_at range; paginate with limit (default 50, max 200) + offset."
      tags:
        - "audit"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Read the OSS plain persistent audit log, newest first. Filter by event_type, outcome, actor_id, subject_id and a created_at range; paginate with limit (default 50, max 200) + offset."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/browser-login":
    get:
      operationId: "identuum-idp-oss.auth.browser_login_form"
      summary: "Render the browser login form (anonymous; the form posts to the matching POST endpoint)."
      tags:
        - "auth"
      security: []
      responses:
        "200":
          description: "Render the browser login form (anonymous; the form posts to the matching POST endpoint)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.auth.browser_login_submit"
      summary: "Submit the browser login form (verifies credentials and establishes a browser session cookie on success)."
      tags:
        - "auth"
      security: []
      responses:
        "200":
          description: "Submit the browser login form (verifies credentials and establishes a browser session cookie on success)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/claim":
    post:
      operationId: "identuum-idp-oss.auth-lifecycle.consume_claim"
      summary: "Consume a claim token and mint the first org_admin for an empty organization. Oracle-hardened — every failure mode returns 200 with success:false. Bounded retry attempts on password-policy failures."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Consume a claim token and mint the first org_admin for an empty organization. Oracle-hardened — every failure mode returns 200 with success:false. Bounded retry attempts on password-policy failures."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/claim/validate":
    get:
      operationId: "identuum-idp-oss.auth-lifecycle.validate_claim"
      summary: "Validate a claim token pre-flight (oracle-hardened — every failure mode returns 200 {valid:false}). When valid, returns the organization name and (for email-bound tokens) the bound target email."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Validate a claim token pre-flight (oracle-hardened — every failure mode returns 200 {valid:false}). When valid, returns the organization name and (for email-bound tokens) the bound target email."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/idp/{id}/callback":
    get:
      operationId: "identuum-idp-oss.oidc-login.oidc_callback"
      summary: "Complete upstream single-provider OIDC login: consume the one-time state, exchange the code for tokens, strictly validate the ID token against the provider JWKS, JIT-provision or match the local user, mint a local session, and 302-redirect to the stored return URL with the session cookie set."
      tags:
        - "oidc-login"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security: []
      responses:
        "200":
          description: "Complete upstream single-provider OIDC login: consume the one-time state, exchange the code for tokens, strictly validate the ID token against the provider JWKS, JIT-provision or match the local user, mint a local session, and 302-redirect to the stored return URL with the session cookie set."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/idp/{id}/login":
    get:
      operationId: "identuum-idp-oss.oidc-login.oidc_login_initiation"
      summary: "Begin upstream single-provider OIDC login: resolve the org's configured OIDC provider by id, fetch its discovery metadata, mint state/nonce/PKCE, persist the OIDCState, and 302-redirect to the provider's authorize URL."
      tags:
        - "oidc-login"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security: []
      responses:
        "200":
          description: "Begin upstream single-provider OIDC login: resolve the org's configured OIDC provider by id, fetch its discovery metadata, mint state/nonce/PKCE, persist the OIDCState, and 302-redirect to the provider's authorize URL."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login":
    post:
      operationId: "identuum-idp-oss.auth.local_login"
      summary: "Local email/password login. Verifies credentials + optional TOTP and returns a session/user-token; password is never echoed in the response or any audit metadata."
      tags:
        - "auth"
      security: []
      responses:
        "200":
          description: "Local email/password login. Verifies credentials + optional TOTP and returns a session/user-token; password is never echoed in the response or any audit metadata."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login/mfa":
    post:
      operationId: "identuum-idp-oss.auth.mfa_verify_login"
      summary: "Two-step MFA verification. For users already enrolled in TOTP; consumes the pending verify-kind session_id from /api/v1/auth/login on the mfa_required path, verifies the TOTP code against the user's persisted MFASecret, and completes the login by issuing the full session + Set-Cookie."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Two-step MFA verification. For users already enrolled in TOTP; consumes the pending verify-kind session_id from /api/v1/auth/login on the mfa_required path, verifies the TOTP code against the user's persisted MFASecret, and completes the login by issuing the full session + Set-Cookie."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login/mfa/enroll/complete":
    post:
      operationId: "identuum-idp-oss.auth.mfa_enroll_complete"
      summary: "Pending-MFA enrolment complete. Verifies the supplied TOTP code against the candidate secret from /initiate, persists mfa_enabled=true + mfa_secret + mfa_recovery_codes onto the user row, marks the pending row consumed, and completes the login by issuing the full session + Set-Cookie access_token (and refresh_token when remember_me was set on the original /login)."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Pending-MFA enrolment complete. Verifies the supplied TOTP code against the candidate secret from /initiate, persists mfa_enabled=true + mfa_secret + mfa_recovery_codes onto the user row, marks the pending row consumed, and completes the login by issuing the full session + Set-Cookie access_token (and refresh_token when remember_me was set on the original /login)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login/mfa/enroll/initiate":
    post:
      operationId: "identuum-idp-oss.auth.mfa_enroll_initiate"
      summary: "Pending-MFA enrolment initiate. Given a session_id minted by /api/v1/auth/login on the mfa_enrollment_required path, generates a fresh TOTP candidate secret + recovery codes and returns the otpauth URL for one-time client QR display."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Pending-MFA enrolment initiate. Given a session_id minted by /api/v1/auth/login on the mfa_enrollment_required path, generates a fresh TOTP candidate secret + recovery codes and returns the otpauth URL for one-time client QR display."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login/webauthn/begin":
    post:
      operationId: "identuum-idp-oss.webauthn.web_authn_login_begin"
      summary: "Begin a WebAuthn login ceremony for an email. Returns a CredentialAssertion + opaque session_id. Anti-enumeration — zero or multiple matches both return a structurally indistinguishable dummy assertion."
      tags:
        - "webauthn"
      security: []
      responses:
        "200":
          description: "Begin a WebAuthn login ceremony for an email. Returns a CredentialAssertion + opaque session_id. Anti-enumeration — zero or multiple matches both return a structurally indistinguishable dummy assertion."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/login/webauthn/finish":
    post:
      operationId: "identuum-idp-oss.webauthn.web_authn_login_finish"
      summary: "Finish a WebAuthn login ceremony. Consumes the session_id query parameter (single-use); the request body is the browser's assertion response. On success issues a user session + Set-Cookie access_token (and refresh_token when remember_me=true on the query). All failure modes collapse to 401 invalid_credentials."
      tags:
        - "webauthn"
      security: []
      responses:
        "200":
          description: "Finish a WebAuthn login ceremony. Consumes the session_id query parameter (single-use); the request body is the browser's assertion response. On success issues a user session + Set-Cookie access_token (and refresh_token when remember_me=true on the query). All failure modes collapse to 401 invalid_credentials."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/logout":
    post:
      operationId: "identuum-idp-oss.auth.logout"
      summary: "Log out the authenticated principal's session (idempotent — unknown / already-revoked sessions return 204)."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Log out the authenticated principal's session (idempotent — unknown / already-revoked sessions return 204)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/organization-lookup":
    get:
      operationId: "identuum-idp-oss.auth.organization_lookup"
      summary: "Public organization-lookup. Returns a safe projection of the organization's wire-visible auth configuration (slug, name, domain, auth_policy, login_url, identity_providers) for the pre-authentication UI email step. 404 on unknown / inactive / soft-deleted organizations. Never carries IdP secrets, internal URLs, or signing-key material."
      tags:
        - "auth"
      security: []
      responses:
        "200":
          description: "Public organization-lookup. Returns a safe projection of the organization's wire-visible auth configuration (slug, name, domain, auth_policy, login_url, identity_providers) for the pre-authentication UI email step. 404 on unknown / inactive / soft-deleted organizations. Never carries IdP secrets, internal URLs, or signing-key material."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/organizations/activate":
    post:
      operationId: "identuum-idp-oss.auth-lifecycle.activate_organization"
      summary: "Complete an organization activation. Sets the org_admin's password, marks the org Active, marks the org_admin's email verified, and consumes the activation token (single-use)."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Complete an organization activation. Sets the org_admin's password, marks the org Active, marks the org_admin's email verified, and consumes the activation token (single-use)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/organizations/activate/{token}":
    get:
      operationId: "identuum-idp-oss.auth-lifecycle.validate_activation_token"
      summary: "Validate an organization-activation token. Returns the org_admin's email + organization id so the UI can render the password-setup form."
      tags:
        - "auth-lifecycle"
      parameters:
        - name: "token"
          in: path
          required: true
          schema:
            type: string
      security: []
      responses:
        "200":
          description: "Validate an organization-activation token. Returns the org_admin's email + organization id so the UI can render the password-setup form."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/password/reset":
    post:
      operationId: "identuum-idp-oss.auth-lifecycle.reset_password"
      summary: "Complete a password reset using a one-time token. On success, all active sessions for the user are revoked."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Complete a password reset using a one-time token. On success, all active sessions for the user are revoked."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/password/reset-request":
    post:
      operationId: "identuum-idp-oss.auth-lifecycle.request_password_reset"
      summary: "Request a password reset link for the supplied email. Anti-enumeration — always 200 regardless of whether the email matches a user, plus a 100–300 ms random delay so timing cannot leak account existence."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Request a password reset link for the supplied email. Anti-enumeration — always 200 regardless of whether the email matches a user, plus a 100–300 ms random delay so timing cannot leak account existence."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/resend-verification":
    post:
      operationId: "identuum-idp-oss.auth-lifecycle.resend_verification"
      summary: "Resend a verification email. Anti-enumeration — always 200 regardless of whether the email matches an unverified user."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Resend a verification email. Anti-enumeration — always 200 regardless of whether the email matches an unverified user."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/session/refresh":
    post:
      operationId: "identuum-idp-oss.auth.session_refresh"
      summary: "Refresh the authenticated principal's session (rotates the session token; old token is invalidated server-side)."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Refresh the authenticated principal's session (rotates the session token; old token is invalidated server-side)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/auth/verify-email":
    get:
      operationId: "identuum-idp-oss.auth-lifecycle.verify_email"
      summary: "Verify an email address via a one-time token. Idempotent — re-clicking the link after success returns 200. Single-use semantics enforced server-side."
      tags:
        - "auth-lifecycle"
      security: []
      responses:
        "200":
          description: "Verify an email address via a one-time token. Idempotent — re-clicking the link after success returns 200. Single-use semantics enforced server-side."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/clients":
    get:
      operationId: "identuum-idp-oss.clients.list_clients"
      summary: "List OAuth clients (safe projection — never exposes client_secret_hash or inline JWKS material)."
      tags:
        - "clients"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List OAuth clients (safe projection — never exposes client_secret_hash or inline JWKS material)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeClient"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.clients.create_client"
      summary: "Create an OAuth client (client_secret returned ONCE in the response body; never stored in cleartext server-side)."
      tags:
        - "clients"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create an OAuth client (client_secret returned ONCE in the response body; never stored in cleartext server-side)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeClient"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/clients/{id}":
    get:
      operationId: "identuum-idp-oss.clients.get_client"
      summary: "Show a single OAuth client by client_id (safe projection)."
      tags:
        - "clients"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single OAuth client by client_id (safe projection)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeClient"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.clients.update_client"
      summary: "Update OAuth client metadata (does not rotate the client_secret)."
      tags:
        - "clients"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update OAuth client metadata (does not rotate the client_secret)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeClient"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.clients.delete_client"
      summary: "Delete an OAuth client."
      tags:
        - "clients"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Delete an OAuth client."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/clients/{id}/secret/regenerate":
    post:
      operationId: "identuum-idp-oss.clients.regenerate_client_secret"
      summary: "Rotate an OAuth client's client_secret (new secret returned ONCE; old secret invalidated immediately)."
      tags:
        - "clients"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Rotate an OAuth client's client_secret (new secret returned ONCE; old secret invalidated immediately)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeClient"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/component":
    get:
      operationId: "identuum-idp-oss.top-level.component_handler"
      summary: "Component discovery: safe OSS product/API capability facts."
      tags:
        - "top-level"
      security: []
      responses:
        "200":
          description: "Component discovery: safe OSS product/API capability facts."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/health/details":
    get:
      operationId: "identuum-idp-oss.top-level.handle_system_health_details"
      summary: "Site-admin runtime info: overall status, build version, and the liveness of the dependencies OSS actually has (database, audit log). Fields OSS cannot know (Redis, an audit queue depth) are ABSENT, never zero-faked. No URLs, credentials, or session material."
      tags:
        - "top-level"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Site-admin runtime info: overall status, build version, and the liveness of the dependencies OSS actually has (database, audit log). Fields OSS cannot know (Redis, an audit queue depth) are ABSENT, never zero-faked. No URLs, credentials, or session material."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys":
    get:
      operationId: "identuum-idp-oss.keys.list_signing_keys"
      summary: "List signing keys (safe projection — never exposes private key material)."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List signing keys (safe projection — never exposes private key material)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeKey"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys/deprecate":
    post:
      operationId: "identuum-idp-oss.keys.deprecate_signing_key"
      summary: "Deprecate a signing key by kid."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Deprecate a signing key by kid."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys/expired":
    delete:
      operationId: "identuum-idp-oss.keys.delete_expired_keys"
      summary: "Purge deprecated-past-expiry signing key rows."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Purge deprecated-past-expiry signing key rows."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys/generate":
    post:
      operationId: "identuum-idp-oss.keys.generate_signing_key"
      summary: "Generate a new signing key and persist it (private key never returned)."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Generate a new signing key and persist it (private key never returned)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeKey"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys/reload":
    post:
      operationId: "identuum-idp-oss.keys.reload_signing_key_not_implemented"
      summary: "Reload key manager (returns 501 in OSS; KeyManager not yet relocated into OSS)."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Reload key manager (returns 501 in OSS; KeyManager not yet relocated into OSS)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/keys/rotate":
    post:
      operationId: "identuum-idp-oss.keys.rotate_signing_key"
      summary: "Rotate signing keys (new active key + retire previous)."
      tags:
        - "keys"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Rotate signing keys (new active key + retire previous)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeKey"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/mfa/disable":
    post:
      operationId: "identuum-idp-oss.auth.mfa_disable_self"
      summary: "Self-service MFA disable. Verifies a fresh TOTP code, one valid recovery code, OR the authenticated user's current password, then clears mfa_enabled / mfa_secret / mfa_recovery_codes on the authenticated user's row. Blocked when the principal is site_admin, org_admin, or in an organization whose mfa_policy is \"required\". On success the user's existing sessions and OAuth refresh tokens are revoked best-effort."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Self-service MFA disable. Verifies a fresh TOTP code, one valid recovery code, OR the authenticated user's current password, then clears mfa_enabled / mfa_secret / mfa_recovery_codes on the authenticated user's row. Blocked when the principal is site_admin, org_admin, or in an organization whose mfa_policy is \"required\". On success the user's existing sessions and OAuth refresh tokens are revoked best-effort."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/mfa/recovery-codes/regenerate":
    post:
      operationId: "identuum-idp-oss.auth.mfa_recovery_codes_regenerate"
      summary: "Self-service MFA recovery-code regeneration. Replaces the authenticated user's stored recovery codes with a fresh list of the same length+count as enrolment and returns the new codes EXACTLY ONCE. MFAEnabled and MFASecret are not modified; the user's existing TOTP enrolment continues to work."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Self-service MFA recovery-code regeneration. Replaces the authenticated user's stored recovery codes with a fresh list of the same length+count as enrolment and returns the new codes EXACTLY ONCE. MFAEnabled and MFASecret are not modified; the user's existing TOTP enrolment continues to work."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/mfa/status":
    get:
      operationId: "identuum-idp-oss.auth.mfa_status"
      summary: "Self-service MFA status read. Returns the safe projection of the authenticated user's MFA configuration: mfa_enabled, recovery_codes_remaining_count, totp_enrolled. Never returns MFA secret, otpauth URL, recovery code values, password hash, tokens, or session material."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Self-service MFA status read. Returns the safe projection of the authenticated user's MFA configuration: mfa_enabled, recovery_codes_remaining_count, totp_enrolled. Never returns MFA secret, otpauth URL, recovery code values, password hash, tokens, or session material."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/roles":
    get:
      operationId: "identuum-idp-oss.rbac.list_my_roles"
      summary: "List the authenticated principal's own role assignments (Authorization Server feature)."
      tags:
        - "rbac"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List the authenticated principal's own role assignments (Authorization Server feature)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/sessions":
    get:
      operationId: "identuum-idp-oss.auth.list_own_active_sessions"
      summary: "Self-service active session/device list. Returns safe metadata for the authenticated user's own active sessions only."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Self-service active session/device list. Returns safe metadata for the authenticated user's own active sessions only."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/sessions/revoke-all":
    post:
      operationId: "identuum-idp-oss.auth.revoke_all_own_sessions"
      summary: "Self-service revoke all sessions/devices. Revokes every active browser session and OAuth refresh token for the authenticated user only."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Self-service revoke all sessions/devices. Revokes every active browser session and OAuth refresh token for the authenticated user only."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/sessions/revoke-current":
    post:
      operationId: "identuum-idp-oss.auth.revoke_current_own_session"
      summary: "Self-service revoke current session/device. Revokes only the authenticated user's current browser session derived from the bound principal."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Self-service revoke current session/device. Revokes only the authenticated user's current browser session derived from the bound principal."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/me/sessions/revoke-others":
    post:
      operationId: "identuum-idp-oss.auth.revoke_other_own_sessions"
      summary: "Self-service revoke other sessions/devices. Revokes every active browser session for the authenticated user except the current session derived from the bound principal."
      tags:
        - "auth"
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Self-service revoke other sessions/devices. Revokes every active browser session for the authenticated user except the current session derived from the bound principal."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/authorize":
    get:
      operationId: "identuum-idp-oss.oauth.authorize"
      summary: "OAuth 2.1 authorize endpoint — validates the request and either redirects to login, prompts for consent, or returns an authorization_code via redirect."
      tags:
        - "oauth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "OAuth 2.1 authorize endpoint — validates the request and either redirects to login, prompts for consent, or returns an authorization_code via redirect."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/consent":
    get:
      operationId: "identuum-idp-oss.oauth.consent_form"
      summary: "Interactive consent form (renders the minimal consent HTML for the authenticated user; echoes the /authorize query parameters through hidden form fields so the POST can resume the exact authorize request)."
      tags:
        - "oauth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Interactive consent form (renders the minimal consent HTML for the authenticated user; echoes the /authorize query parameters through hidden form fields so the POST can resume the exact authorize request)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.oauth.consent_submit"
      summary: "Submit interactive consent — on accept, redirects back to the original /authorize flow with an authorization_code; on deny, returns the access_denied error envelope."
      tags:
        - "oauth"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Submit interactive consent — on accept, redirects back to the original /authorize flow with an authorization_code; on deny, returns the access_denied error envelope."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/introspection":
    post:
      operationId: "identuum-idp-oss.oauth.introspection"
      summary: "RFC 7662 token introspection (returns {\"active\":false} for revoked or unknown tokens)."
      tags:
        - "oauth"
      security:
        - oauthClientAuth: []
      responses:
        "200":
          description: "RFC 7662 token introspection (returns {\"active\":false} for revoked or unknown tokens)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/register":
    post:
      operationId: "identuum-idp-oss.oauth.dcr_register"
      summary: "RFC 7591 Dynamic Client Registration. site_admin or a valid Authorization Bearer IAT may register. client_secret returned ONCE."
      tags:
        - "oauth"
      security:
        - cookieAuth: []
        - bearerAuth: []
      responses:
        "201":
          description: "RFC 7591 Dynamic Client Registration. site_admin or a valid Authorization Bearer IAT may register. client_secret returned ONCE."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_dcrResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/revoke":
    post:
      operationId: "identuum-idp-oss.oauth.revoke"
      summary: "RFC 7009 token revocation (idempotent — unknown tokens return 200 per spec)."
      tags:
        - "oauth"
      security:
        - oauthClientAuth: []
      responses:
        "200":
          description: "RFC 7009 token revocation (idempotent — unknown tokens return 200 per spec)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oauth/token":
    post:
      operationId: "identuum-idp-oss.oauth.token"
      summary: "OAuth 2.1 token endpoint (authorization_code, refresh, client_credentials, token exchange)."
      tags:
        - "oauth"
      security:
        - oauthClientAuth: []
      responses:
        "200":
          description: "OAuth 2.1 token endpoint (authorization_code, refresh, client_credentials, token exchange)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oidc/frontchannel-logout":
    get:
      operationId: "identuum-idp-oss.oidc.frontchannel_logout"
      summary: "OIDC Front-Channel Logout 1.0 iframe endpoint (clears the browser cookie + revokes the cookie-resolved session, then renders a minimal iframe-safe HTML response)."
      tags:
        - "oidc"
      security: []
      responses:
        "200":
          description: "OIDC Front-Channel Logout 1.0 iframe endpoint (clears the browser cookie + revokes the cookie-resolved session, then renders a minimal iframe-safe HTML response)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oidc/logout":
    get:
      operationId: "identuum-idp-oss.oidc.end_session"
      summary: "OIDC RP-initiated logout / end_session endpoint (clears the browser cookie + revokes the cookie-resolved session; honours post_logout_redirect_uri when allowed by the registered client)."
      tags:
        - "oidc"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "OIDC RP-initiated logout / end_session endpoint (clears the browser cookie + revokes the cookie-resolved session; honours post_logout_redirect_uri when allowed by the registered client)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/oidc/userinfo":
    get:
      operationId: "identuum-idp-oss.oidc.userinfo_get"
      summary: "OIDC Core §5.3.1 userinfo (GET form)."
      tags:
        - "oidc"
      security:
        - bearerAuth: []
      responses:
        "200":
          description: "OIDC Core §5.3.1 userinfo (GET form)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.oidc.userinfo_post"
      summary: "OIDC Core §5.3.1 userinfo (POST form — accepts the bearer access token in either the Authorization header or the access_token form parameter)."
      tags:
        - "oidc"
      security:
        - bearerAuth: []
      responses:
        "200":
          description: "OIDC Core §5.3.1 userinfo (POST form — accepts the bearer access token in either the Authorization header or the access_token form parameter)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations":
    get:
      operationId: "identuum-idp-oss.organizations.list_organizations"
      summary: "List organizations (paginated). site_admin sees every org; org_admin gets a single-row list scoped to their own org."
      tags:
        - "organizations"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List organizations (paginated). site_admin sees every org; org_admin gets a single-row list scoped to their own org."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.organizations.create_organization"
      summary: "Create an organization."
      tags:
        - "organizations"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create an organization."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/current":
    get:
      operationId: "identuum-idp-oss.organizations.get_current_organization"
      summary: "Show the authenticated principal's current organization (resolved from the principal's organization_id)."
      tags:
        - "organizations"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show the authenticated principal's current organization (resolved from the principal's organization_id)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/export-candidates":
    get:
      operationId: "identuum-idp-oss.organizations.list_organization_export_candidates"
      summary: "List organizations for cross-system (IDP -> AG) linking/import planning. Safe, narrow projection — NOT the Enterprise SIEM audit-export feature. site_admin sees every non-deleted org (active + inactive); org_admin gets a single-row list scoped to their own org."
      tags:
        - "organizations"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List organizations for cross-system (IDP -> AG) linking/import planning. Safe, narrow projection — NOT the Enterprise SIEM audit-export feature. site_admin sees every non-deleted org (active + inactive); org_admin gets a single-row list scoped to their own org."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_organizationExportCandidate"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}":
    get:
      operationId: "identuum-idp-oss.organizations.get_organization"
      summary: "Show a single organization by id (site_admin OR same-org org_admin)."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single organization by id (site_admin OR same-org org_admin)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.organizations.update_organization"
      summary: "Update an organization (lifecycle + policy fields)."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update an organization (lifecycle + policy fields)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.organizations.delete_organization"
      summary: "Soft-delete an organization."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Soft-delete an organization."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/admin-recovery-candidates":
    get:
      operationId: "identuum-idp-oss.organizations.list_org_admin_recovery_candidates"
      summary: "List the org_admin accounts of a tenant org for operator-driven recovery (reset-MFA card). site_admin only."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List the org_admin accounts of a tenant org for operator-driven recovery (reset-MFA card). site_admin only."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/domains":
    get:
      operationId: "identuum-idp-oss.organization-domains.list_organization_domains"
      summary: "List an organization's domains (safe projection — never exposes the verification-token hash)."
      tags:
        - "organization-domains"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List an organization's domains (safe projection — never exposes the verification-token hash)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganizationDomain"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.organization-domains.add_organization_domain"
      summary: "Add a domain to an organization (issues a verification token whose hash is stored; the raw token is returned ONCE)."
      tags:
        - "organization-domains"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Add a domain to an organization (issues a verification token whose hash is stored; the raw token is returned ONCE)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganizationDomain"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/domains/{domain_id}":
    delete:
      operationId: "identuum-idp-oss.organization-domains.delete_organization_domain"
      summary: "Remove a domain from an organization."
      tags:
        - "organization-domains"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "domain_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Remove a domain from an organization."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/domains/{domain_id}/primary":
    post:
      operationId: "identuum-idp-oss.organization-domains.set_primary_organization_domain"
      summary: "Set an organization's primary domain."
      tags:
        - "organization-domains"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "domain_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Set an organization's primary domain."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganizationDomain"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/domains/{domain_id}/verify":
    post:
      operationId: "identuum-idp-oss.organization-domains.verify_organization_domain"
      summary: "Verify a domain by presenting the previously-issued verification token."
      tags:
        - "organization-domains"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "domain_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Verify a domain by presenting the previously-issued verification token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganizationDomain"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/identity-provider":
    get:
      operationId: "identuum-idp-oss.organization-identity-provider.get_organization_identity_provider"
      summary: "Read the organization's single OIDC identity provider configuration. client_secret is never included."
      tags:
        - "organization-identity-provider"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Read the organization's single OIDC identity provider configuration. client_secret is never included."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_types_IdentityProviderResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.organization-identity-provider.update_organization_identity_provider"
      summary: "Update the organization's single OIDC identity provider. The client_secret is re-encrypted only if a new one is supplied; omitting it preserves the stored secret."
      tags:
        - "organization-identity-provider"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update the organization's single OIDC identity provider. The client_secret is re-encrypted only if a new one is supplied; omitting it preserves the stored secret."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_types_IdentityProviderResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.organization-identity-provider.create_organization_identity_provider"
      summary: "Create the organization's single upstream OIDC identity provider (OSS basic single-provider login). Rejects a second provider (one per org) and any non-oidc type (ldap/ad are CE)."
      tags:
        - "organization-identity-provider"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create the organization's single upstream OIDC identity provider (OSS basic single-provider login). Rejects a second provider (one per org) and any non-oidc type (ldap/ad are CE)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_types_IdentityProviderResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.organization-identity-provider.delete_organization_identity_provider"
      summary: "Delete the organization's single OIDC identity provider."
      tags:
        - "organization-identity-provider"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Delete the organization's single OIDC identity provider."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_identityProviderDeleteResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/protocol-settings":
    get:
      operationId: "identuum-idp-oss.organization-protocol-settings.get_organization_protocol_settings"
      summary: "Read the per-organization DCR + SCIM enable/disable settings (source field = explicit when a row exists, default when the system fallback applies)."
      tags:
        - "organization-protocol-settings"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Read the per-organization DCR + SCIM enable/disable settings (source field = explicit when a row exists, default when the system fallback applies)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_organizationProtocolSettingsResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.organization-protocol-settings.put_organization_protocol_settings"
      summary: "Upsert the per-organization DCR + SCIM enable/disable settings. Both booleans are required."
      tags:
        - "organization-protocol-settings"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Upsert the per-organization DCR + SCIM enable/disable settings. Both booleans are required."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_organizationProtocolSettingsResponse"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/resend-activation":
    post:
      operationId: "identuum-idp-oss.organizations.resend_activation"
      summary: "Re-issue and return a pending organization's activation token (site_admin operator retrieval path). Re-dispatches via email when a notifier is wired; OSS echoes the token in the response."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Re-issue and return a pending organization's activation token (site_admin operator retrieval path). Re-dispatches via email when a notifier is wired; OSS echoes the token in the response."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/restore":
    post:
      operationId: "identuum-idp-oss.organizations.restore_organization"
      summary: "Restore a previously soft-deleted organization."
      tags:
        - "organizations"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Restore a previously soft-deleted organization."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeOrganization"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/roles":
    get:
      operationId: "identuum-idp-oss.rbac.list_org_roles"
      summary: "List roles defined in an organization (Authorization Server feature)."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List roles defined in an organization (Authorization Server feature)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.rbac.create_org_role"
      summary: "Create an org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create an org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/roles/{role_id}":
    get:
      operationId: "identuum-idp-oss.rbac.get_org_role"
      summary: "Show a single org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.rbac.update_org_role"
      summary: "Update an org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update an org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.rbac.delete_org_role"
      summary: "Delete an org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Delete an org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/roles/{role_id}/scopes":
    post:
      operationId: "identuum-idp-oss.rbac.add_role_scope"
      summary: "Add a scope to an org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Add a scope to an org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/roles/{role_id}/scopes/{scope_name}":
    delete:
      operationId: "identuum-idp-oss.rbac.remove_role_scope"
      summary: "Remove a scope from an org role."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
        - name: "scope_name"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Remove a scope from an org role."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/service-accounts":
    get:
      operationId: "identuum-idp-oss.service-accounts.list_service_accounts"
      summary: "List service accounts in an organization (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List service accounts in an organization (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.service-accounts.create_service_account"
      summary: "Create a service account in an organization (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create a service account in an organization (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/organizations/{id}/service-accounts/with-client":
    post:
      operationId: "identuum-idp-oss.service-accounts.create_service_account_with_client"
      summary: "Create a service account bundled with an OAuth client (one-shot issuance; the OAuth client_secret is returned ONCE)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create a service account bundled with an OAuth client (one-shot issuance; the OAuth client_secret is returned ONCE)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/profile":
    get:
      operationId: "identuum-idp-oss.profile.get_profile"
      summary: "Get the authenticated principal's own user profile (safe projection — never exposes password_hash or recovery codes)."
      tags:
        - "profile"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Get the authenticated principal's own user profile (safe projection — never exposes password_hash or recovery codes)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/revoke":
    post:
      operationId: "identuum-idp-oss.sessions.revoke_own_session"
      summary: "Revoke a session owned by the authenticated user. Idempotent — unknown / already-revoked / cross-user session ids all return 200 success without disclosure (anti-enumeration). Current session revocation is allowed and signalled via current_revoked:true."
      tags:
        - "sessions"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Revoke a session owned by the authenticated user. Idempotent — unknown / already-revoked / cross-user session ids all return 200 success without disclosure (anti-enumeration). Current session revocation is allowed and signalled via current_revoked:true."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/scope-templates":
    get:
      operationId: "identuum-idp-oss.scope-templates.list_scope_templates"
      summary: "List scope templates for the authenticated principal's organization (Authorization Server feature)."
      tags:
        - "scope-templates"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List scope templates for the authenticated principal's organization (Authorization Server feature)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeScopeTemplate"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.scope-templates.create_scope_template"
      summary: "Create a scope template (Authorization Server feature)."
      tags:
        - "scope-templates"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create a scope template (Authorization Server feature)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeScopeTemplate"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/scope-templates/{id}":
    get:
      operationId: "identuum-idp-oss.scope-templates.get_scope_template"
      summary: "Show a single scope template by id (Authorization Server feature)."
      tags:
        - "scope-templates"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single scope template by id (Authorization Server feature)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeScopeTemplate"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.scope-templates.update_scope_template"
      summary: "Update a scope template (Authorization Server feature)."
      tags:
        - "scope-templates"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update a scope template (Authorization Server feature)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeScopeTemplate"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.scope-templates.delete_scope_template"
      summary: "Delete a scope template (Authorization Server feature)."
      tags:
        - "scope-templates"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Delete a scope template (Authorization Server feature)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/service-accounts/{id}":
    get:
      operationId: "identuum-idp-oss.service-accounts.get_service_account"
      summary: "Show a service account (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a service account (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.service-accounts.update_service_account"
      summary: "Update a service account (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update a service account (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.service-accounts.delete_service_account"
      summary: "Delete a service account (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Delete a service account (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/service-accounts/{id}/disable":
    post:
      operationId: "identuum-idp-oss.service-accounts.set_active_service_account_disable"
      summary: "Disable a service account (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Disable a service account (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/service-accounts/{id}/enable":
    post:
      operationId: "identuum-idp-oss.service-accounts.set_active_service_account_enable"
      summary: "Enable a previously-disabled service account (handler enforces site_admin OR same-org membership)."
      tags:
        - "service-accounts"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Enable a previously-disabled service account (handler enforces site_admin OR same-org membership)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/sessions":
    get:
      operationId: "identuum-idp-oss.sessions.list_own_sessions"
      summary: "List the authenticated user's own sessions. Safe projection — never returns session tokens, selectors, validator hashes, or refresh-token material. Cross-tenant isolation — site_admin role returns 403 by design."
      tags:
        - "sessions"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List the authenticated user's own sessions. Safe projection — never returns session tokens, selectors, validator hashes, or refresh-token material. Cross-tenant isolation — site_admin role returns 403 by design."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users":
    get:
      operationId: "identuum-idp-oss.users.list_users"
      summary: "List users (paginated via page + page_size query params; safe projection — never exposes password_hash or recovery codes)."
      tags:
        - "users"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List users (paginated via page + page_size query params; safe projection — never exposes password_hash or recovery codes)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.users.create_user"
      summary: "Create a user (password hashed before persistence; password and password_hash never appear in the response body or audit metadata)."
      tags:
        - "users"
      security:
        - cookieAuth: []
      responses:
        "201":
          description: "Create a user (password hashed before persistence; password and password_hash never appear in the response body or audit metadata)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/bulk":
    post:
      operationId: "identuum-idp-oss.users.bulk_create_users"
      summary: "Bulk-create up to 50 users in one org (synchronous, best-effort; returns per-item created + failed results). Reuses the single-create path per user (same validation, org-scoping, secure defaults, UUIDv7)."
      tags:
        - "users"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Bulk-create up to 50 users in one org (synchronous, best-effort; returns per-item created + failed results). Reuses the single-create path per user (same validation, org-scoping, secure defaults, UUIDv7)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}":
    get:
      operationId: "identuum-idp-oss.users.get_user"
      summary: "Show a single user by id (safe projection — never exposes password_hash or recovery codes)."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Show a single user by id (safe projection — never exposes password_hash or recovery codes)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    put:
      operationId: "identuum-idp-oss.users.update_user"
      summary: "Update a user."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Update a user."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    delete:
      operationId: "identuum-idp-oss.users.delete_user"
      summary: "Soft-delete a user."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Soft-delete a user."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}/approve":
    post:
      operationId: "identuum-idp-oss.users.approve_user"
      summary: "Approve a pending self-registered user (clears the public_registration hold so the user can authenticate)."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Approve a pending self-registered user (clears the public_registration hold so the user can authenticate)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}/recovery/reset-mfa":
    post:
      operationId: "identuum-idp-oss.users.reset_user_mfa"
      summary: "Reset a user's MFA (clears mfa_enabled, mfa_secret, and mfa_recovery_codes; idempotent)."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Reset a user's MFA (clears mfa_enabled, mfa_secret, and mfa_recovery_codes; idempotent)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}/restore":
    post:
      operationId: "identuum-idp-oss.users.restore_user"
      summary: "Restore a previously soft-deleted user."
      tags:
        - "users"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Restore a previously soft-deleted user."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oss_handlers_safeUser"
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}/roles":
    get:
      operationId: "identuum-idp-oss.rbac.list_roles_for_user"
      summary: "List a user's role assignments."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List a user's role assignments."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
    post:
      operationId: "identuum-idp-oss.rbac.assign_role_to_user"
      summary: "Assign a role to a user."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Assign a role to a user."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/users/{id}/roles/{role_id}":
    delete:
      operationId: "identuum-idp-oss.rbac.remove_role_from_user"
      summary: "Remove a role from a user."
      tags:
        - "rbac"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
        - name: "role_id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Remove a role from a user."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/validate":
    get:
      operationId: "identuum-idp-oss.auth.validate_session"
      summary: "Validate the caller's session (access_token cookie or Authorization: Bearer). Returns the authenticated user + role for UI session-guard consumption. 401 on any verification failure."
      tags:
        - "auth"
      security:
        - cookieAuth: []
        - bearerAuth: []
      responses:
        "200":
          description: "Validate the caller's session (access_token cookie or Authorization: Bearer). Returns the authenticated user + role for UI session-guard consumption. 401 on any verification failure."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/webauthn/credentials":
    get:
      operationId: "identuum-idp-oss.webauthn.list_web_authn_credentials"
      summary: "List the authenticated user's WebAuthn credentials. Safe projection — never exposes the raw credential id, public key, or attestation object."
      tags:
        - "webauthn"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "List the authenticated user's WebAuthn credentials. Safe projection — never exposes the raw credential id, public key, or attestation object."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/webauthn/credentials/{id}":
    delete:
      operationId: "identuum-idp-oss.webauthn.delete_web_authn_credential"
      summary: "Delete one of the authenticated user's WebAuthn credentials by its database id. Ownership-enforced — a credential the caller does not own returns 404 (no enumeration)."
      tags:
        - "webauthn"
      parameters:
        - name: "id"
          in: path
          required: true
          schema:
            type: string
      security:
        - cookieAuth: []
      responses:
        "204":
          description: "Delete one of the authenticated user's WebAuthn credentials by its database id. Ownership-enforced — a credential the caller does not own returns 404 (no enumeration)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/webauthn/register/begin":
    post:
      operationId: "identuum-idp-oss.webauthn.web_authn_register_begin"
      summary: "Start a WebAuthn passkey registration ceremony for the authenticated user. Returns the browser's PublicKeyCredentialCreationOptions plus an opaque session_id for the matching /register/finish call. Challenge is short-lived (5 minutes) and single-use."
      tags:
        - "webauthn"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Start a WebAuthn passkey registration ceremony for the authenticated user. Returns the browser's PublicKeyCredentialCreationOptions plus an opaque session_id for the matching /register/finish call. Challenge is short-lived (5 minutes) and single-use."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/api/v1/webauthn/register/finish":
    post:
      operationId: "identuum-idp-oss.webauthn.web_authn_register_finish"
      summary: "Complete a WebAuthn passkey registration ceremony. Consumes the session_id query parameter (single-use); the request body is the browser's attestation response. On success persists a credential row bound to the authenticated user and emits a webauthn_credential_registered audit event."
      tags:
        - "webauthn"
      security:
        - cookieAuth: []
      responses:
        "200":
          description: "Complete a WebAuthn passkey registration ceremony. Consumes the session_id query parameter (single-use); the request body is the browser's attestation response. On success persists a credential row bound to the authenticated user and emits a webauthn_credential_registered audit event."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/health":
    get:
      operationId: "identuum-idp-oss.top-level.health_handler"
      summary: "Health probe (liveness + readiness)."
      tags:
        - "top-level"
      security: []
      responses:
        "200":
          description: "Health probe (liveness + readiness)."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
  "/system/info":
    get:
      operationId: "identuum-idp-oss.top-level.system_info_handler"
      summary: "System info: deployment metadata + module identifier."
      tags:
        - "top-level"
      security: []
      responses:
        "200":
          description: "System info: deployment metadata + module identifier."
        default:
          description: "Error response — see identuum-idp-oss source for exact status/body per failure mode."
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    cookieAuth:
      type: apiKey
      in: cookie
      name: "identuum_session"
      description: "Browser session cookie backing a cookie-authenticated Principal (site_admin, org_admin, or any authenticated user, depending on the operation's actual role requirement — see its summary)."
    oauthClientAuth:
      type: http
      scheme: basic
      description: "OAuth 2.1 client authentication (client credentials / RFC 7592 management token) at client-management and token endpoints."
  schemas:
    oss_handlers_dcrResponse:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.dcrResponse). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_identityProviderDeleteResponse:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.identityProviderDeleteResponse). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_organizationExportCandidate:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.organizationExportCandidate). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_organizationProtocolSettingsResponse:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.organizationProtocolSettingsResponse). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeAPIResource:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeAPIResource). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeBackchannelDelivery:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeBackchannelDelivery). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeClient:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeClient). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeKey:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeKey). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeOrganization:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeOrganization). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeOrganizationDomain:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeOrganizationDomain). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeScopeTemplate:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeScopeTemplate). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_handlers_safeUser:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.handlers.safeUser). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
    oss_types_IdentityProviderResponse:
      type: object
      description: "Response shape defined by the OSS handler package (schema record: oss.types.IdentityProviderResponse). Field-level shape is not modeled here — see the identuum-idp-oss Go source for the authoritative struct."
