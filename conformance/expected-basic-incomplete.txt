# THE-SECOND-LOGIN — the oidcc-basic plan's INCOMPLETE-module floor is EMPTY.
#
# THE-PKCE-DECISION recorded oidcc-prompt-login and oidcc-max-age-1 here:
# both stalled in WAITING because the OP re-issued on the live session
# instead of forcing re-authentication. THE-SECOND-LOGIN honors prompt=login
# and max_age (a fresh login mints a new session, so auth_time advances), and
# both modules now run to completion — their entries moved OUT of this floor
# AND of expected-failures-basic.json, nothing else moved.
#
# With no module lines below, run.sh treats run-test-plan's own exit code as
# authoritative for the basic plan (the expected-failures/skips files remain
# the floor for conditions). If a module ever stalls again, record it here
# deliberately — one exact module name per non-comment line, sorted — and the
# incomplete-set equality floor re-engages (rule CONFORMANCE-FLOOR-1).
