{
  "schema_version": "ledger-amendments.v1",
  "base_commit": "c5dd4a77ea4ca46ff84a323876c1e2f4205b3179",
  "changes": [
    {
      "rule_id": "CI-RECORD-HONEST-1",
      "change_class": "test_fingerprint_changed",
      "reason": "Supply the caller's expected gate and plan to the existing Judge test calls after its signature change. All 25 assertion sites and the rule sentence remain unchanged."
    },
    {
      "rule_id": "MFA-STEPUP-PASSWORD-1",
      "change_class": "rule_removed",
      "reason": "Retire the unreachable password helper and its dedicated test after d2ba656 committed mutation-proven coverage of surviving disarm state and error identities. The live disarm path refuses password-only proofs and never calls this helper."
    },
    {
      "rule_id": "MINT-REACHABILITY-1",
      "change_class": "test_fingerprint_changed",
      "reason": "THE-SIX-SMALL-ONES, OSS 1 (2026-09-16): reach_test.go gained the subtest that declares scripts/** no-reach WITH its proof — the image recipe never copies a scripts/ path into the runtime stage nor runs one, and the toolchain's embed patterns name none — re-measured on every run; three real gate scripts classify SKIPPABLE, the sibling's scripts/ and e2e-full/scripts/ stay REQUIRED. The sentence is unchanged and still true: undeclared paths, source, go.mod, go.sum, deployment and the harness all still mint. Rehashed 81b909d38520 -> 9d4c4fbe2158. THE-EIGHT-QUICK-ONES, OSS 3 (2026-09-16), same unwitnessed base: reach_test.go gained the subtest that declares the SIBLING's Makefile no-reach (identuum-ui/Makefile SKIPPABLE, this module's Makefile and the ui's Dockerfile still REQUIRED) with the proof on fixtures both ways — a RUN that invokes make, a runner-stage COPY from the context and a package.json script calling make each FAIL by name, an absent recipe fails closed — and on the real sibling checkout where present; the sibling's-namespace subtest's reaching list names identuum-ui/Dockerfile where it named identuum-ui/Makefile. Rehashed 9d4c4fbe2158 -> cb972906162a. THE-LEGATTUS-THAT-LEAVES-THE-TREE-ALONE (2026-09-16), same unwitnessed base: reach_test.go gained the subtest that declares .legattus-policy.json — Legattus v0.1.4's one in-tree file — no-reach for THIS repository with the scripts/** proofs re-measured (the runtime stage copies only --from= artifacts and names no Legattus path; no embed pattern names it), while a nested copy, a .bak and the sibling's policy stay REQUIRED. Rehashed cb972906162a -> 8201ac891dc8."
    },
    {
      "rule_id": "TOOLS-NO-REACH-1",
      "change_class": "test_fingerprint_changed",
      "reason": "THE-SIX-SMALL-ONES, OSS 1 (2026-09-16): closure_test.go's list of paths that must still mint used scripts/gate-witness.sh as its example of a reaching non-tool path; scripts/** is now declared no-reach with proof, so the example is deployment/docker-compose.dev.yml. The assertion — an undeclared file still mints, the gate-program entries excuse only the listed programs — is unchanged. Rehashed ddc3cfcf004a -> f1486e3f3761."
    },
    {
      "rule_id": "GRYPE-SUBJECT-1",
      "change_class": "test_fingerprint_changed",
      "reason": "THE-SCANNER-THAT-SAYS-WHAT-IT-SAW (2026-09-16): subject_test.go's directory cases now judge the coverage predicate on the CycloneDX inventory grype wrote beside the scan (-inventory): the covered premise passes on an inventory with nothing under an ignored path, the (b) red is an inventory naming a component under the gitignored stray, a directory scan without an inventory and an empty inventory are CANNOT-EVALUATE, and -coverage-only judges an inventory against the tree with no scanner. The sentence is unchanged and still true: an image subject reports both predicates not applicable, a directory keeps both with full teeth on its own tree, and a -root that is not the subject is refused. Rehashed bf8fa9f4ed01 -> 5b86174c063c. THE-THREE-SMALL-ONES-OSS (2026-09-19), same unwitnessed base: subject_test.go's two directory cases now expect the evidence line to name a directory subject by its directory's BASE NAME (`subject directory:<name>`) and assert the subject's absolute path is NOT on the line — the line is copied into the tracked GATE-RUN.txt of a public repository and carried the operator's machine path; the image case is untouched (`subject image:identuum-ui:verify`). Red first through a go overlay of HEAD's subject.go (both directory subtests FAILED on the absolute path), green on the edit. The sentence is unchanged and still true: ResolveDir still compares absolute values and refuses a -root that is not the subject; only the label changed. Rehashed 5b86174c063c -> f6763bdbb6b3."
    },
    {
      "rule_id": "LEDGER-REBASE-DERIVES-BASE-1",
      "change_class": "test_fingerprint_changed",
      "reason": "THE-SIX-SMALL-ONES, OSS 2 (2026-09-16): rebase_test.go gained the subtest that a declaration the witness consumed is NAMED by the re-base with the refusal the next verify will make, while nothing is removed and every declaration still survives byte-for-byte. The sentence is unchanged and still true: only base_commit is written, an invalid manifest is refused, the derived base is re-measured at verify, and verify never re-bases. Rehashed 10f4bf006b94 -> faaf599948eb."
    }
  ]
}
